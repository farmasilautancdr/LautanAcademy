# MEMORY.md

# MEMORY
*Update autonomously. Keep ultra-brief. Move old data to ARCHIVE.md.*

[STACK]: Vue 3, Vite, Tailwind | Node.js, Express, Postgres (Supabase) | JWT auth, native bcrypt.
[STRUCTURE]: This working copy lives at `C:\Projects\my-project\LautanAcademy` — frontend repo (root, `origin/master`, github.com/farmasilautancdr/LautanAcademy). Backend is a separate sibling repo checked out inside it at `farmasilautancdr-lautan-academy-backend-/` (`origin/main`, github.com/farmasilautancdr/farmasilautancdr-lautan-academy-backend-). A second, older frontend-only clone also exists at `C:\Users\Client\LautanAcademy` (same remote, kept in sync — not a separate project). Branch -> `master`(frontend)/`main`(backend), direct commits, no feature-branch workflow.
[RULES]: Bilingual (EN/MS) for UI strings. No new frameworks/libs without asking. Match existing file styles. GAS→Postgres migration COMPLETE (2026-08-11) — Postgres is sole source of truth, no code path anywhere talks to GAS (full cutover detail in ARCHIVE.md).
[DECISIONS]: Switched to native bcrypt (was bcryptjs) to prevent single-thread blocking during concurrent logins.
[LESSONS] (full incident narrative for each in ARCHIVE.md):
- Any `item.disabled ? null : someHandler` ternary in a `@click` must call `someHandler()` explicitly — a bare reference inside a ternary silently never fires (bit the whole sidebar once).
- Never hardcode padding/margin against a fixed-position element whose size is role/data-driven — measure it (`ResizeObserver` + CSS var), don't guess a fixed px value.
- Any new router/nav work must keep Vue Router's `scrollBehavior` in mind — no inner scroll container in this app, `window` itself is it.
- Any future `results`-table topic split must check both `videoHoursByTopic` AND `contentHoursByTopic` — 3 topic-namespace sources share that table, not 2.
- DB-stored ids from node-pg (bigserial) come back as strings — don't use them as numeric Map keys without matching types on both sides.
- Grade quizzes only against the authoritative server-side question set/count, never trust a client-submitted array length or id list.
- `useCpdHours.js`'s `videoHoursByTopic`/`contentHoursByTopic` expect specific field casing (`topic`/`hours` lowercase for video_trainings, `Topic`/`QuizRequired`/`Hours` for content) — a mock/test payload with wrong casing silently misclassifies Video Training rows as Module Quiz instead of erroring.
[FRAGILITY]:
- In-app Drive resource upload is **permanently** out of scope (Google service accounts have zero storage quota on a regular Drive folder — real Google platform limit). User decided 2026-08-11 not to pursue Shared Drive/OAuth delegation. Supervisors upload to Drive directly, outside the app, permanently. `POST /resources/upload` stays in backend, dormant/correct/unreachable — leave as-is.
- `BACKEND_URL`/`VITE_API_URL` are hardcoded/baked-in constants (vanilla `index.html` and the Vite build respectively) — a Railway URL change needs a manual edit + rebuild, not just an env var change.
- Vanilla `index.html`'s real production hosting is **GitHub Pages** (`https://farmasilautancdr.github.io/LautanAcademy/`, built from `master`), undocumented until 2026-08-11. Depends on `.nojekyll` at repo root staying in place — don't delete it, and don't assume a red Pages Actions run means it's dead without checking first.
- Local dev backend on `C:\Users\Client`'s machine can't reach Supabase's Direct-connection host (IPv6-only, this network has no IPv6 egress) — use the Session Pooler connection string (`aws-0-<region>.pooler.supabase.com`) for any local `npm run dev`, not the Direct host. Railway production already uses the pooler; this is a local-machine-only gap, not a prod issue.
- No separate test/staging DB exists for this project — local dev and "testing" both point at the same production Supabase DB. Be careful with any destructive/bulk-write test (see Annual Data Reset in ARCHIVE.md for why that endpoint's real delete path has never actually been executed).

[DONE 2026-09-08/09] Frontend vitest + @vue/test-utils harness setup (1st of 3 items deferred from the grading-suite plan). Spec: `docs/superpowers/specs/2026-09-08-frontend-vitest-setup-design.md`. Plan: `docs/superpowers/plans/2026-09-08-frontend-vitest-setup.md` (both checkboxes now marked done, commit `f3228db`). `vitest@^3.x` used instead of the plan's `^5.0.0` (vite^5.2.8 pin blocks vitest 5); `@pinia/testing@^0.1.7` pinned (latest needs pinia≥4, project's on pinia^2.1.7) + explicit `createSpy: vi.fn`. `npm test`: 1/1 passing (`tests/App.smoke.test.js`).

[DONE 2026-09-09] Frontend CI workflow (2nd of 3 deferred items) — bounded-path change (brainstorming skill, no spec doc). New `.github/workflows/ci.yml`: push/PR to `master`, `npm ci`+`npm test`+`npm run build` in `lautan-academy-frontend/`, Node 24. Frontend-only (vanilla has no tests, backend is a separate repo with no test infra on its default branch at the time — see below, now resolved by the merge). Verified live via `gh run watch` — green. Commit `fbbbbc5`.

[DONE 2026-09-09] Same-session bug fixes (user-reported, unplanned): (1) CSV exports (`SupervisorReportsView.vue`, `SupervisorDashboard.vue`, `QuizHistoryView.vue`) never guarded a cell value starting with `=+-@` — Excel/Sheets parses it as a formula on open, producing `#NAME?` in place of manager-typed Recommendations/Performance Gaps text. Fixed: leading-apostrophe guard (OWASP CSV-injection mitigation) in all three `csvEscape()` functions, commit `f9041b8`. First attempt wrongly targeted `Code_v1.35.gs` (long-dead GAS file, no code path uses it — see [RULES]) before the real Postgres-backed write path was found; that dead file was then deleted entirely, commit `f1d546b`. (2) Added a Region column (reverse outlet→area-id lookup off `AREAS`) to the module-quiz CSV export, commit `88fc81f`.

[NOTE 2026-09-09] Local backend `main` (in `farmasilautancdr-lautan-academy-backend-/`) was found 8 commits behind `origin/main` this session — the entire grading-test-suite PR (#1, merged 2026-09-08) hadn't been pulled locally. Fast-forwarded clean, no divergence/conflict. Same lesson class as the frontend `master` drift noted 2026-09-08 below — `git fetch` + check both repos' divergence before assuming either local checkout is current, don't rely on this file's "last known state" alone.

[ACTIVE TASK — RESUME HERE, design not yet approved] Backend auth/lockout tests (3rd, last deferred item). Architectural path (superpowers:brainstorming) — 7 `/auth/*` endpoints, several with deliberately shared lockout counters across endpoints (already patched once for a real cross-endpoint bug per `auth.js`'s own inline comments). Design presented to user, awaiting explicit approval before writing the spec doc + invoking writing-plans.
Design as presented:
- Files: `tests/auth.{staffLogin,managerLogin,managerRegister,verifyPin,masterLogin,masterPinAdmin}.test.js` (one per endpoint/pair) + `tests/auth.lockoutSharing.test.js` (cross-endpoint shared-counter invariants).
- Coverage: correctness + lockout for staff-login/manager-login/manager-register/verify-pin/master-login/master-reset-supervisor-pin (5-fail/5-min lockout via `rate_limits`, per `middleware/rateLimit.js`); authz-only for `rotate-master-pin` (no PIN comparison, nothing to brute-force — just `requireAuth`+`requireScope('supervisor')`).
- Lockout mechanics tested via real HTTP requests (attempt counting) + direct `rate_limits.expires_at` DB manipulation for window-expiry (no real 5-min sleeps).
- Cross-endpoint invariants to cover: manager-login's master-PIN-fallback path shares its counter with verify-pin + manager-register for the same role; a role's registered personal-credential login uses a *different* counter than that role's master-PIN fallback; verify-pin's `resources` role has its own independent counter; master-login/master-reset-supervisor-pin keyed independently per-username/session.
- Schema fix (user-chosen, "fold into schema.sql"): `master_users` table exists live only via a standalone one-off script (`scripts/migrate-add-master-users.js`), never applied by `scripts/apply-test-schema.js` — the disposable test DB currently can't test master-login at all. **This exact gap was already flagged as a known, deliberately-deferred issue back in Subsystem F (2026-08-11, see ARCHIVE.md)** — this task finally resolves it by folding that script's `create table if not exists master_users(...)` block into `sql/schema.sql` itself.
- New test helpers needed in `tests/helpers/db.js`: `insertStaffWithPin`, `insertManagerPin`, `insertManagerCredential`, `insertMasterUser`, `mintManagerToken`, `mintMasterToken`, `expireRateLimit(key)`, `clearRateLimit(key)`, plus cleanup additions for the new tables touched.
[NEXT STEPS ON RESUME]: Get the user's explicit yes/changes on the design above, then write the spec to `docs/superpowers/specs/`, self-review, get spec approval, then invoke writing-plans (do NOT skip straight to implementation — architectural path).

[PENDING] (still open, not started):
- Supabase Storage free-tier usage — `POST /content/upload` caps 20MB/file, no total-usage guard. Check Supabase dashboard → Storage → Usage before this becomes a real problem; add a guard if picked up.
- Outlet region reassign rule — when an outlet's area/region changes, Master should be able to freely reassign it to any area with no restriction. User-stated 2026-08-11, not yet coded — apply when touching `store_outlets.area_id` reassignment logic.
- iOS Safari <16.4 doesn't support `fetch keepalive` — Module Quiz's tab/app-close abandon-catch silently doesn't fire on older iPhones (in-app nav-away guard still works everywhere). User: leave as-is unless revisited (would need a `sendBeacon` fallback + backend support for unauthenticated-body requests).

[STATE as of 2026-09-09]: Frontend `master` at `f3228db` — vitest harness + CI workflow done, CSV `#NAME?`/Region fixes done, dead `Code_v1.35.gs` deleted. Backend `main` at `cb1da27` (fast-forwarded this session, was 8 commits behind) — grading-test-suite merged, no other change yet. Active: backend auth/lockout test design (see ACTIVE TASK above), awaiting approval.
[NEXT STEPS]: Get design approval → spec → writing-plans → implement. Before claiming any future work "done", verify against real `git status`/`git log` rather than trusting this file alone.
