# MEMORY.md

# MEMORY
*Update autonomously. Keep ultra-brief. Move old data to ARCHIVE.md.*

[STACK]: Vue 3, Vite, Tailwind | Node.js, Express, Postgres (Supabase) | JWT auth, native bcrypt.
[STRUCTURE]: Frontend repo `lautan-academy-frontend/` (this repo). Backend is a SEPARATE sibling repo `C:\Users\Hafiz\projects\lautan-academy-backend` (not `/backend` in this repo, despite CLAUDE.md). Branch -> `master`, direct commits (no feature-branch workflow used).
[RULES]: Bilingual (EN/MS) for UI strings. No new frameworks/libs without asking. Match existing file styles. GAS→Postgres migration COMPLETE (2026-08-11) — Postgres is sole source of truth, GAS web app deployment decommissioned, no code path anywhere talks to GAS.
[CUTOVER]: Full cutover from GAS completed 2026-08-11 — 3 steps, sequential, each user-confirmed: (1) confirmed nobody hand-edits the Google Sheet anymore, everyone uses the app; (2) GAS web app deployment archived/disabled via Apps Script's Deploy → Manage deployments — verified dead (`GET` on the old GAS URL now 404s); (3) CLAUDE.md rule 6 + this file's [RULES] line updated to declare Postgres authoritative. The underlying Sheet/script data itself was left untouched (not deleted), only the web app deployment killed — reversible if ever needed, but not expected to be.
[DECISIONS]: Switched to native bcrypt to prevent single-thread blocking during concurrent logins.
[MISTAKES]:
- `standard_questions.id` string/int mismatch caused 0 scoring (Fixed).
- Grading trusted client arrays instead of DB authoritative row count (Fixed).
- Area Manager scoping bypassed in vanilla login (Fixed).
- Vanilla's AI Practice reveal always showed wrong (backend stopped sending `correct` on `/quiz/redeem` mid-session as a security fix, vanilla's `handleChoice()`/`exitQuizWithAutosave()` weren't updated to match — silent regression, no error thrown) (Fixed 2026-08-10, commit `98fd393`).
- `extractYouTubeId()` in `routes/videoTraining.js` used an unanchored regex `.match()` against the raw URL string — a link containing the right substring anywhere (e.g. `https://evil.example/?u=youtube.com/watch?v=...`) would pass. Caught by automated security review. Fixed to parse as a real URL and check `hostname` exactly (Fixed 2026-08-12/13, commit `c42e91d`; `VideoWatchView.vue`'s client-side copy fixed to match for consistency).
- `AppSidebar.vue`'s Coming-Soon-gated nav item used `@click="item.disabled ? null : navigate"` — a ternary expression, which Vue evaluates inline rather than invoking as a click handler, so `navigate` (a function reference) was never actually called. This silently broke click-to-navigate for **every** sidebar nav item, not just the gated one — caught by live-browser verification (Playwright), not by `npm run build` (no type error, builds clean either way). Fixed to `navigate()` (Fixed 2026-08-13, commit `fa4bb7d`). **Lesson: any future `item.disabled ? null : someHandler` pattern must call the function explicitly (`someHandler()`), not reference it bare, inside a ternary.**
- Quiz History (and every manager-tier results view: `AreaManagerDashboard.vue`, `OutletManagerResultsView.vue`, `WarehouseManagerResultsView.vue`) always showed wrong-answer question/answer review text in **English regardless of app language** — user-reported via screenshot 2026-08-17 ("answer in ms but question and answer still in en"). Root cause: the 3 grading endpoints (`POST /results`, `/ai-results`, `/video-results` in `data.js`) always snapshotted the `_en` columns into `wrong_answers`/`ai_wrong_answers` when saving a wrong answer, regardless of which language the staff was actually viewing (`QuizView.vue` itself already switches display correctly by `useI18n().locale` — that choice just never reached the backend). Fixed (Fixed 2026-08-17, backend + frontend, not yet committed at time of writing): `wrong_answers`/`ai_wrong_answers` schema made bilingual (`question`/`chosen`/`correct` each split into `_en`+`_ms`, migration `scripts/migrate-wrong-answers-bilingual.js`, run against prod Supabase — renames existing single-language columns to `_en`, preserving existing data as English since that's all that was ever written, adds nullable `_ms`); all 3 grading endpoints now capture both languages from the already-bilingual source tables (`standard_questions`/`video_questions`/AI's `questions_json`, all of which already had `_ms` columns, just weren't being read); `toResponse()` exposes both as `'Question Text En'`/`'Question Text Ms'` etc; all 4 frontend consumers get a shared `bilingual(w, field)` helper that picks by current locale, falling back to En when Ms is null (permanent gap for any row saved before this fix — old history can't be backfilled, flagged explicitly to user). Verified end-to-end: local dev server + disposable `TESTOUTLET|TESTSTAFF` staff JWT (minted via `issueToken`, same no-real-PIN pattern every prior subsystem used) against real `standard_questions` data, all 20 deliberately-wrong answers for one topic round-tripped through the real `/data/results` endpoint and came back with correctly paired En/Ms text (checked directly in Postgres), test rows cleaned up after (`wrong_answers`/`results`/`sessions`); `npm run build` clean, EN/MS key-parity clean (791/side). **Noted, not fixed (pre-existing, unrelated):** True/False questions store `''` (not `null`) in their unused 3rd/4th option slots, so the `?? '(no answer)'` fallback in all 3 grading endpoints doesn't catch them — pre-dates this fix, same behavior before and after, out of scope.
[FRAGILITY]: In-app Drive resource upload UI permanently out of scope (Google Workspace service account storage quota limit) — user decided 2026-08-11 to skip Shared Drive/OAuth delegation route entirely, not pursuing. Supervisors upload directly to Drive outside the app, permanently.

[ACTIVE TASK]: i18n (EN/BM) rollout — Phase 1 + Phase 2 (all 4 batches, 32 views/components) COMPLETE. Batch 3 (Warehouse+Area Manager) and Batch 4 (Supervisor) manual browser click-through + BM native-speaker spot-check done live 2026-08-10 (user confirmed both OK, no issues) — closes the last outstanding item. i18n rollout fully done end to end, nothing left open.

[ACTIVE TASK]: Vanilla GAS-bridge removal — requested 2026-08-10/11, DONE. Closed `index.html`'s last GAS dependency: `GET /questions` replaces the GAS bulk fetch (drops `correct` — closes a real answer-key leak GAS's public `doGet()` had), staff roster moved to per-outlet `GET /auth/staff-roster` (extended to return `{name, idNote}`, fixes an IDNote-disambiguation gap in both vanilla AND Vue's login dropdowns), Standard Quiz reveal now calls `POST /questions/:id/check` live (mirrors the AI Practice fix). A related live bug found first and fixed separately ahead of this (commit `98fd393`): AI Practice's reveal always showed wrong because the backend already stripped `correct` from `/quiz/redeem` earlier in this session but vanilla never got the matching fix. Also fixed a stored-XSS flagged by automated review (staff name/idNote interpolated raw into innerHTML — added `escapeHtml()`, scoped to the one line touched). Spec: `docs/superpowers/specs/2026-08-10-vanilla-gas-bridge-removal-design.md`. Plan: `docs/superpowers/plans/2026-08-10-vanilla-gas-bridge-removal.md`. Backend commits `e6eabff` (idNote), frontend `98fd393`..`91f7ead`.

**Two real production incidents surfaced while shipping this, both resolved:**
1. Vanilla `index.html` is actually served live via **GitHub Pages** (`https://farmasilautancdr.github.io/LautanAcademy/`, `master` branch) — not documented anywhere in MEMORY.md/SCOPE_TRACKER before this. Earlier in this same session, a failing "pages build and deployment" GitHub Actions run was misdiagnosed as dead/irrelevant (looked like leftover from before the Vue migration) and Pages was disabled via Settings → Pages → Source → None on that assumption — wrong call, made without asking what depended on it first. Turned out the Pages *build* had actually been failing on every push regardless (Jekyll trying to process a plain static-HTML repo, not a Jekyll site) — so no working site was taken down by disabling it, but it also meant staff's bookmarked URL had already been broken before this session touched anything. Fixed for real with a `.nojekyll` file at repo root (tells Pages to skip the Jekyll build, serve files as-is) — the correct, permanent fix, commit `91f7ead`.
2. Railway's GitHub auto-deploy did not pick up the backend push (`e6eabff`) — production kept serving the old `GET /auth/staff-roster` response shape (bare name strings) for several minutes after push+poll, breaking the staff/manager name dropdown live (`r.name` on a string is `undefined`). Fixed with a manual `railway up` from `lautan-academy-backend` — deployed within ~2 min once triggered manually. Root cause of the auto-deploy not firing not investigated further (out of time-pressure); if this recurs, check Railway's GitHub service-source connection first (see existing Known Fragility entry — this project has hit this exact failure mode before).

[DONE, WITH ONE STUB] Master User / Super Admin role + Control Panel — requested 2026-08-10, decomposed into 8 sub-projects (each own spec/plan cycle), build order agreed with user. B-H only cover 7 real capabilities (PIN reset, purge, maintenance, audit logs, backup/export, sessions, impersonation) — `overrides` tab (`MasterPanel.vue`'s Permission Overrides) was Subsystem B's original scope but got narrowed out during B's brainstorm (`docs/superpowers/specs/2026-08-11-master-subsystem-b-design.md`: "if a future subsystem needs broader bypass, it gets its own brainstorm/spec cycle — not bundled here") and that follow-up never happened. Still shows "Coming Soon", `ENABLED_TABS` in `MasterPanel.vue` excludes it. **User decision 2026-08-11: left for later use, not building now.**
- A-G (Master role/auth/panel shell, PIN reset, data purge, maintenance kill-switch, audit logs, DB backup/export, active sessions+force-logout) — all DONE 2026-08-10/11, full detail moved to ARCHIVE.md to cut token bloat now the initiative is closed. Specs/plans: `docs/superpowers/specs/2026-08-1{0,1}-master-*-design.md`, `docs/superpowers/plans/2026-08-1{0,1}-master-*.md`.
- H. Outlet/role impersonation switcher — DONE 2026-08-11. Master gets a real scoped JWT for a target `{scopeType, scopeKey}` (same shape a real login produces — no new identity table), tagged `impersonated: true`, 30-minute TTL (not the usual 12h). Target grain: staff = `OUTLET|NAME` (must match a real `staff_roster` row); Outlet/Warehouse/Area Manager = outlet/location/area id (these roles have no per-person account server-side — role+outlet IS the account). **Supervisor and Master excluded** — Supervisor has no narrower account to view as (unscoped `scopeKey='ALL'`), Master-as-Master is meaningless. View-only, enforced in exactly one place: a new check inside `requireAuth` itself (not a separate middleware — the spec proposed one, but `requireAuth` is the actual single choke point every authenticated route already passes through) blocks any non-GET request carrying the `impersonated` claim, backend-wide, no per-route edits. `issueToken()` reworked to take an optional `{expiresInMinutes, impersonatedBy}` — TTL now expressed in minutes everywhere (was hours), since `jsonwebtoken`'s `expiresIn` string (parsed by the `ms` package) doesn't reliably parse a fractional-hour string like `"0.5h"`. New `sessions.impersonated_by` column (nullable, same pattern as Subsystem G's `revoked_by`) tags impersonation-issued session rows — visible/revocable in Active Sessions like any other session, no new tracking mechanism. New `routes/masterImpersonate.js`: `POST /master/impersonate/start` (validates target is real before issuing — staff_roster lookup, or outlet/area-id membership check against `config/areas.js`/a local `WAREHOUSE_LOCATIONS` list) and `POST /master/impersonate/end` (revokes the session row via the exact same path Subsystem G's force-logout already uses — `revoked_at`, `addRevokedSid` — so manual Exit and a Master force-revoke via Active Sessions converge on one code path). Both actions audit-logged (`impersonation.start`/`impersonation.end`). Frontend: real constraint found — `api/client.js`'s `getToken()` and every existing view read `localStorage['lautan_token']`/`staff`/`manager` directly, so making every existing dashboard/history view work unmodified under an impersonated identity meant writing into those same keys, which would otherwise clobber a real concurrent staff/manager session in the same browser. Fixed with a stash-and-restore contained entirely to `store/auth.js`: `startImpersonation()` snapshots the current real session into a separate `lautan_stash` key before overwriting the live ones; `exitImpersonation()` (manual Exit button, or auto-triggered by `api/client.js`'s new 401-while-impersonating handler — same dynamic-import-to-avoid-circular-import pattern the maintenance 503 handler already established) restores the stash. `MasterImpersonation.vue` (target picker, wired into the `impersonation` tab already reserved since Subsystem A) and `ImpersonationBanner.vue` (fixed "Viewing as X — Exit" bar in `App.vue`, shown whenever `auth.impersonating`) are new; 6 existing views (DashboardView, ModuleQuizView, ManageStaffPanel, OutletManagerDashboard, WarehouseManagerDashboard, AreaManagerReviewsView) each got their primary mutating control (Join/Start Quiz, Create Quiz, Manage Staff add/reset/remove, File/Edit Report) gated behind `!auth.impersonating` — UX only, the backend's `requireAuth` check is the real boundary. Verified: full curl round-trip (token claims correct, GET allowed/POST 403'd, immediate revocation on end, both audit rows, all 5 validation/exclusion rejections, auth gating) using disposable throwaway staff+master test accounts (deleted after, same caution as every prior subsystem), `npm run build` clean after every task, EN/MS key-parity clean — **caught and fixed a real bug during this**: `impersonatingNotice` landed in the wrong i18n namespace twice while editing (`outletManagerResultsView` instead of `outletManagerDashboard`, and a missed key in `warehouseManagerDashboard`'s `ms.json`), both caught by the parity script and fixed before commit. User confirmed live in browser ("working") — Master Panel's "View As" tab, impersonation banner, hidden mutating UI, session isolation. Backend commits `da37569`, `7799d8a`, `6b6d853`. Frontend commits `d0780c7`..`599c80a`. Spec: `docs/superpowers/specs/2026-08-11-master-subsystem-h-design.md`. Plan: `docs/superpowers/plans/2026-08-11-master-subsystem-h.md`. **This closes the full 8-subsystem Master User / Super Admin Control Panel initiative (A-H) — all built, verified, and shipped.**
[DONE, NOT PUSHED] Outlet & Area Management — requested 2026-08-11 (user: "can Master edit outlet list without editing code"), full brainstorm/spec/plan/execute cycle, 12-task plan fully implemented and verified same session. New `areas`+`store_outlets` Postgres tables (soft-delete only via `active` bool, no rename endpoint — `code`/`id` are referenced by free text elsewhere) replace the `AREAS`/`OUTLET_LIST`/`WAREHOUSE_LOCATIONS` arrays that were hardcoded and duplicated across backend `config/areas.js` + **14** frontend files (more than the 6 originally scoped — grep before deleting the file caught 4 extra real importers: `PurgeReportsContentPanel.vue`, `SupervisorDashboard/ReportsView/StaffComparisonView.vue`). Public `GET /outlets`/`GET /areas` (no auth, same pattern as `/questions`) back every login/register dropdown; Master-only `/master/outlets` CRUD backs a new "Outlets & Areas" Control Panel tab. New shared composable `useOutlets.js` — this codebase's first composable, justified by collapsing 14 duplicate copies into one fetch. **Real blocker hit and fixed during Task 1:** this Supabase project already has an unrelated `outlets` table (with dependent `staff`/`quizzes`/`attempts`/`manager_reviews` tables — a different app's schema entirely, none matching this repo's own `schema.sql`) — user confirmed rename over drop, table is `store_outlets` not `outlets` (HTTP routes/JSON fields/frontend names all still say "outlets", only the Postgres identifier differs). Also corrected an inherited miscount: the old code's own comment claimed "49 outlet codes", actual array has 50 — real seed is 9 areas, 50 retail + 4 warehouse = 54 total. Area id/label split (`areas.id='R1'`, `areas.label='AMIRUL'`, displayed as `"R1 - AMIRUL"`) fixes the old design where the combined string doubled as both. Verified: full curl round-trip on every backend task (incl. duplicate/409, area-deactivate-blocked-while-active-outlets/400, no-auth/401) using disposable throwaway master accounts + a live growth-scenario simulation (Master adds outlet via API → immediately selectable via public `GET /outlets`, zero redeploy — the actual feature requested), `npm run build` clean after every frontend task, EN/MS key-parity clean, grep-confirmed zero leftover `config/areas.js` references (code and stale comments both). **Not yet pushed** — user chose to keep 9 (`lautan-academy`) + 6 (`lautan-academy-backend`) commits local for review before triggering the real Vercel/Railway deploys. Spec: `docs/superpowers/specs/2026-08-11-outlet-management-design.md`. Plan: `docs/superpowers/plans/2026-08-11-outlet-management.md`. **Still needed: live browser click-through** — no browser tool was available this session (user declined the Chrome extension), so UI verification stopped at build-clean + API-level checks; dropdowns/panel should get a manual spot-check before or right after pushing.

Each of B-H needed its own brainstorm/spec/plan cycle before building — matches subsystem A's process. Current auth architecture: no roles table — roles are hardcoded scopeType strings baked into the JWT (`staff_retail`, `outlet_manager`, etc via `middleware/auth.js` + `routes/auth.js`), bcrypt PIN in `manager_pins`/`manager_credentials`. `master_users` (A), `audit_log` (E), and `sessions` (G, extended by H's `impersonated_by` column) are the tables built outside that original model.
[DECISIONS]:
- `vue-i18n` (Composition API, `legacy: false`) wired app-wide. `src/i18n/index.js`, locale files `src/i18n/locales/{en,ms}.json` (flat, one top-level namespace per view). `LanguageSwitcher.vue` toggles + persists to `localStorage['lautan_lang']`.
- BM text authored directly by Claude (no paid translation API) — native-speaker spot-check done live 2026-08-10, confirmed OK.
- Phase 1 scope (done, 9 commits, `521b194`..`0405f7a`): infra, LanguageSwitcher, 6 standalone login/register views (staff/manager/area-manager/supervisor login, manager/area-manager register), AppSidebar.vue (desktop + mobile nav, role labels, logout).
- `LanguageSwitcher.vue` redesigned post-ship per user feedback: segmented pill (was single toggle button), then flags (🇬🇧/🇲🇾) dropped — text-only EN/BM per explicit instruction. Commits `7852f61`, `3f21739`.
- Phase 2 Batch 1 (done, 6 commits `c25f489`..`1576f65`, plan `docs/superpowers/plans/2026-08-10-i18n-phase2-batch1.md`): DashboardView, ModuleQuizView, QuizView, ResultView, QuizHistoryView, ResourcesView. Also closed the Phase 1 spec's flagged "open risk": `QuizView.vue`'s separate local `lang` ref (drove question `_en`/`_ms` text) is gone, replaced by the shared `useI18n().locale` + `<LanguageSwitcher />` — question-content language and UI-chrome language are now one switch, not two.
- Vue-i18n pluralization pattern established for count strings: `en.json` uses `"{count} thing | {count} things"` (pipe syntax, vue-i18n picks form from `t(key, count)`); `ms.json` uses one form only (BM doesn't inflect for plural) — vue-i18n falls back to it regardless of count, this is correct, not a gap.
- `ResourcesView.vue`'s manager-role header label reuses Phase 1's `sidebar.roleOutletManager`/etc. keys instead of a duplicate copy — pattern to reuse for any future view needing the same 4 role labels.
- Verified (Batch 1): `npm run build` clean after every task, EN/BM key-parity check clean (212 keys each side, no missing either direction), user confirmed live in browser (dashboard/module quiz/quiz taking incl. reconciled lang switch/result/quiz history/resources) — all switching correctly both directions.
- Phase 2 Batch 2 (done, 5 commits `72ebd4a`..`dc38d83`, plan `docs/superpowers/plans/2026-08-10-i18n-phase2-batch2.md`): OutletManagerDashboard, OutletManagerResultsView, ManageStaffPanel (shared component — also used by WarehouseManagerDashboard, so that usage's Manage Staff section is now bilingual too as a side effect; WarehouseManagerDashboard's own page chrome is still Batch 3), OutletManagerStaffView, OutletManagerStaffReviewView. All 4 views' "Outlet Manager" header label reuses Phase 1's `sidebar.roleOutletManager` key (same pattern as Batch 1's ResourcesView). `confirm()` dialog strings (End quiz code, Remove staff) also translated via `t()`. Verified: `npm run build` clean after each of 5 tasks, EN/BM key-parity clean (278 keys/side), user confirmed live in browser (all 4 views + ManageStaffPanel, all switches working correctly).
- Phase 2 Batch 3 (done, 5 commits `18f4fd8`..`874b9c4`, plan `docs/superpowers/plans/2026-08-10-i18n-phase2-batch3.md`): WarehouseManagerDashboard, WarehouseManagerResultsView, WarehouseManagerStaffView (all 3 reuse `sidebar.roleWarehouseManager`, mirrors Outlet Manager pattern), AreaManagerDashboard (the region-wide **Staff Results** page despite the filename — not "Area Manager dashboard" in the nav sense), AreaManagerReviewsView (the **Assessment** page — File a Report + Submissions). Area Manager's header eyebrow (`{{ managerLabel }}`) is real session data (the picked area id, e.g. "R2 - HAZWANI"), not a static role literal like Outlet/Warehouse Manager's — deliberately left untranslated, along with its practically-unreachable `|| 'Area Manager'` fallback default. `skillLevel` badge (HIGH/MEDIUM/LOW) in AreaManagerReviewsView also deliberately untranslated — it's the literal value persisted to `reports.Skill Level`, must match GAS's enum and other views showing the same DB field. Verified: `npm run build` clean after each of 5 tasks, EN/BM key-parity clean (358 keys/side).
- Phase 2 Batch 4 — last batch, Supervisor group (done, 5 commits `839be3b`..`aad5d8e`, plan `docs/superpowers/plans/2026-08-10-i18n-phase2-batch4.md`): SupervisorDashboard (Company-wide activity), SupervisorAddResourcesView (Add Resources), SupervisorReportsView (Cluster Reports), SupervisorStaffComparisonView (Staff Comparison), SupervisorManagerAccessView (Manager Access) — all 5 reuse `sidebar.roleSupervisor` for the header (static literal here, unlike Batch 3's Area Manager). `SupervisorDashboard.vue`'s per-row `kind` tag (Standard/AI Practice) is client-only display data (never persisted) so it WAS translated, computed reactively — different call from Batch 3's persisted `skillLevel`. `SupervisorReportsView.vue`'s CSV export header row deliberately stays English-only (downloadable data artifact, not on-screen UI). `SupervisorStaffComparisonView.vue`'s attempts count reuses Batch 1's pluralization pattern. `SupervisorManagerAccessView.vue`'s 3 role-row labels reuse Phase 1's `sidebar.roleOutletManager`/`roleWarehouseManager`/`roleAreaManager` via a `labelKey` field replacing the old hardcoded `label` string. Verified: `npm run build` clean after each of 5 tasks, EN/BM key-parity clean (434 keys/side, final count). **Phase 2 is now fully complete — all 32 views/components from the original i18n spec (`docs/superpowers/specs/2026-08-09-i18n-design.md`) are migrated.**
[DONE, NOT PUSHED] Two small frontend asks (2026-08-17): (1) Supervisor Cluster Reports gets a Topic filter (`SupervisorReportsView.vue`) — new `topicFilter` select, cascades under Region/Outlet (topics computed from the region+outlet-filtered list, resets on either changing), CSV export unchanged since it already reads from `filtered`. (2) CPD-hour indicator surfaced to staff wherever a topic is shown for Video Training/Pharmacist Courses (`VideoTrainingListView.vue`, `PharmacistCoursesListView.vue` list rows, `VideoWatchView.vue`/`ReadingView.vue` detail pages) — `hours` was already in the API response (Supervisor's own management view already showed it), just never rendered on the staff-facing side. New pluralized key `cpdHourValue` per view namespace ("{hours} CPD hour | {hours} CPD hours", BM single form "{hours} jam CPD"). Pure frontend, no backend/schema changes. Verified: `npm run build` clean (106 modules), EN/MS key-parity clean (791 keys/side, 0 missing either direction). **Not pushed** — left for user to push when ready.
[NEXT STEPS]: i18n rollout fully closed — no open items. Spec: `docs/superpowers/specs/2026-08-09-i18n-design.md`. Plans: Phase 1 `docs/superpowers/plans/2026-08-09-i18n-phase1.md`, Phase 2 Batch 1 `docs/superpowers/plans/2026-08-10-i18n-phase2-batch1.md`, Batch 2 `docs/superpowers/plans/2026-08-10-i18n-phase2-batch2.md`, Batch 3 `docs/superpowers/plans/2026-08-10-i18n-phase2-batch3.md`, Batch 4 `docs/superpowers/plans/2026-08-10-i18n-phase2-batch4.md`. Await user for next task.
[CAVEAT]: No test framework in this frontend (no vitest/jest) — verification is build + manual click-through, not automated tests.
[DONE, PUSHED] Browse Courses reading quiz + CPD — requested 2026-08-17/18, full brainstorm/spec/plan/execute cycle, 12-task plan, all executed+verified 2026-08-18. Supervisor can opt individual Content ("Knowledge") entries into a reading-gate: mark `quiz_required`, set real per-entry `hours` (like Video Training, not a flat rate — Content entries vary too much in length), author a question bank (`content_questions`, field-for-field copy of `video_questions`, same Supervisor CRUD pattern shipped 2026-08-17). Retail staff see a "Take Quiz" action only on ready (`QuizReady` — has >=1 active question) quiz-required entries, route to new `ContentReadingView.vue` ("I've read this" gate, mirrors `ReadingView.vue`), submit through new `POST /data/content-results` (mirrors `POST /video-results`), graded attempt lands in the shared `results` table like Module Quiz/Video Training. Warehouse staff and every manager tier see marked entries exactly as any other Content entry — zero UI change for them. AI Practice completely untouched — still generates from any Content topic (marked or not) via the existing "Create Quiz From This" hand-off, deliberately kept as a separate mechanism (ephemeral/manager-initiated vs this feature's permanent/staff-initiated). `cpdHoursThisYear()` (backend `data.js`) and `hoursByStaff()` (frontend `useCpdHours.js`) both became 4-source calcs: Video Training (real hours, every attempt stacks, unchanged) + **Content quiz (new: real hours, capped to first attempt per topic per year)** + Module Quiz (flat 1hr, capped, unchanged from the 2026-08-17 fix) + AI Practice (flat 0.25hr, uncapped, unchanged). Video Training deliberately NOT capped (user-confirmed) — only Module Quiz and Content quiz cap to first-attempt-per-topic-per-year.

**Real bug caught mid-execution, before it shipped:** Task 5's first draft of the rewritten `cpdHoursThisYear()` accidentally dropped the pre-existing AI Practice source entirely (3-way instead of 4-way) — caught by re-reading the function against the version it was replacing, before running the live verification curl. Fixed immediately in both the code and the plan doc (so the plan doesn't preserve the same bug for future reference). Live-verified afterward with all 4 sources present in one test (2 content-quiz attempts capped to 2.5h, 1 module-quiz attempt =1h, 2 AI Practice attempts =0.5h uncapped) — returned exactly 4.0.

**Second real issue caught mid-execution:** the plan's Task 7 diffs for wiring the new `contentHoursByTopic` map into the 4 manager-tier CPD call sites (`QuizHistoryView.vue`, `OutletManagerResultsView.vue`, `AreaManagerDashboard.vue`, `SupervisorStaffComparisonView.vue`) had guessed wrong local variable names for 2 of the 4 files during planning (assumed `outletScopedResults`/`outletScopedAiResults` for both `OutletManagerResultsView.vue` and `AreaManagerDashboard.vue`; the real names were `standardHistory`/`aiHistory` for the former). Caught by re-grepping each file's actual current `cpdSummary`/`cpdHoursThisYear` computed line immediately before editing, rather than trusting the plan text — both the code and the plan doc were corrected. Lesson reinforced: a plan's per-file diffs for files not read fresh in the same session are a guess, not a fact, even when written carefully — always re-verify against the live file right before applying.

Verified end-to-end: backend curl round-trips per task using disposable `TESTOUTLET|TESTSTAFF`/`TESTOUTLET|TESTWHSTAFF` staff + `ALL`-scope Supervisor JWTs (minted via `issueToken()`, no real PINs) — 400 validation (hours<=0, unknown topic, MCQ/TF option-count), `QuizReady` flips true only once a question exists, `DELETE` blocked on a topic's last question, same-day dedup no-ops without inserting a second `results` row, retail-only 403 for `staff_warehouse` tokens, full happy-path (create entry+question → staff answers correctly → CPD credited by the entry's real hours) all confirmed live against real prod Supabase, every test row cleaned up after (confirmed via count query). Frontend: `useCpdHours.js`'s `hoursByStaff()` mock-tested standalone (module+content+video mixed scenario, matches the backend's math exactly) via `node -e` import, `npm run build` clean after every task (12/12), EN/MS key-parity clean throughout (865 keys/side final, 0 missing either direction). **Live browser click-through done** (no Playwright MCP available this session, so driven via a standalone `node`+`playwright` npm script — installed ad hoc in the scratchpad dir, not added to either repo — using the system-installed Chrome, `channel: 'chrome'`, headless; same minted-JWT/`localStorage`-injection pattern every prior subsystem's browser verification has used, no real PINs): Supervisor logged in via injected token, filled the real Add Resources form (topic/category/title/body, checked "Quiz required," set 3.5 hours), submitted, confirmed the entry listed; went to Manage Content Quiz, picked the topic, authored a real MCQ question, confirmed it listed. Retail staff (injected token) saw exactly one "Take Quiz" action on Browse Courses and only on that entry; opened it, confirmed the real body text and "3.5 CPD hours" rendered; clicked "I've read this," answered correctly, submitted, landed on the real "Well done!" result page; Quiz History showed "3.5 / 60 hrs". Retook the same entry same day with a deliberately wrong answer — CPD stayed at 3.5, confirming the cap holds through the real UI, not just the API. Warehouse staff (injected token) saw the entry listed with zero "Take Quiz" gate. All 9 assertions passed on the first fully-corrected run (2 earlier runs caught real selector bugs in my own test script, not app bugs — a `<details>`-collapsed Knowledge entry needs its `<summary>` clicked before the nested "Take Quiz" link is interactable, and the reading page's `onMounted` content fetch needed an explicit wait before asserting on its text — both fixed in the script, not the app). All disposable `content`/`content_questions`/`results`/`sessions` rows cleaned up after, confirmed zero leftover. Spec: `docs/superpowers/specs/2026-08-17-content-reading-quiz-design.md`. Plan: `docs/superpowers/plans/2026-08-18-content-reading-quiz.md` (both files self-corrected in place during execution per the 2 issues above, so they now match the shipped code exactly). Backend commits `181846d`..`3068af4` (5 commits), pushed to `origin/main`. Frontend commits `6613a45`..`a3a5cb1` (10 commits, incl. 2 plan-doc-fix commits), pushed to `origin/master`.
[PENDING] Automated test suite — discussed 2026-08-11, deferred to future, not started. Needs: backend `vitest`/`node --test` + `supertest`, separate test DB (NOT prod Supabase — a curl test already overwrote the live Supervisor PIN once, see ARCHIVE.md subsystem B), seed/teardown per test; frontend `vitest` + `@vue/test-utils`; GitHub Actions CI w/ Postgres service container. Priority order if picked up: grading logic (`standard_questions`/`ai_quizzes` scoring, already bit us once) → auth/lockout → rest. User: leave as future plan, no scope/spec cycle started yet.
[DONE, NOT PUSHED] Video/Reading quiz question admin — requested 2026-08-17, full brainstorm/spec/plan/execute cycle, 7-task plan, all executed+verified 2026-08-17. Gives Supervisor in-app CRUD (add/edit/delete) over `video_questions` (the quiz bank behind Video Training + Pharmacist Courses), replacing manual-Supabase-insert-only. Three new Supervisor-only routes on the existing `videoQuestionsRouter` in `routes/videoTraining.js`: `POST /video-questions` (validates `topic` against real `video_trainings` rows — no FK exists, a typo would silently orphan the question — and supports both MCQ (4 options, `correct` 0-3) and True/False (2 options, `correct` 0-1, opt3/opt4 forced to `''` server-side regardless of client input)), `PATCH /video-questions/:id` (full-row overwrite, same validation), `DELETE /video-questions/:id` (blocks with 400 if it's the last question for that topic — `GET /video-trainings`/`/pharmacist` only list a course if its topic has >=1 question, so deleting the last one would silently vanish the course from staff view). New standalone `SupervisorManageQuizQuestionsView.vue` (topic dropdown sourced from real courses only, list+add/edit form, same visual pattern as `SupervisorAddResourcesView.vue`), new sidebar nav item under Browse Courses group (mobile bottom nav gets it automatically via the shared `flatItems`). Hard delete, no soft-delete (`status` column stays `'active'` always — never had another value in real data). Verified: full curl round-trips per route (401/403 no-auth and non-Supervisor, 400 validation incl. unknown topic and out-of-range `correct`, 400 last-question-delete-block, 200 happy paths, TF row's opt3/4 confirmed `''` in DB), `npm run build` clean (107 modules), EN/MS key-parity clean, Vite dev server confirmed serving the new component with no compile error. **No live browser click-through** — no Playwright/browser tool available this session; UI wiring is code-reviewed and build-clean but not clicked through live, same gap several prior subsystems flagged. Spec: `docs/superpowers/specs/2026-08-17-video-quiz-question-admin-design.md`. Plan: `docs/superpowers/plans/2026-08-17-video-quiz-question-admin.md`. Backend commits `7f3dcfa`, `1c0d2e1`, `bbe4a8b`. Frontend commits `346f2a7`, `a3b3b61`. **Not pushed** — committed locally only, matches this repo's convention of batching pushes.

[DONE, NOT PUSHED] Flip Video Training/Pharmacist Courses/CPD Hours live for real staff — requested 2026-08-17 ("finishing video/material training, open up for real user testing along with cpd hour"). Removed the `auth.impersonating` Coming-Soon gate that every Video Training/CPD subsystem above deliberately shipped with (see their own entries: "not yet flipped live for real staff"). 6 files: `AppSidebar.vue` (Video Training + Pharmacist Courses nav items, was ternary'd to `disabled:true` for real staff), `DashboardView.vue`, `QuizHistoryView.vue`, `OutletManagerResultsView.vue`, `AreaManagerDashboard.vue`, `SupervisorStaffComparisonView.vue` (each had a `v-if="auth.impersonating"` real-CPD-section / `v-else` Coming-Soon split — collapsed to just the real section, Coming-Soon branch deleted). Pure UI-gate removal, no logic/backend change — the underlying features (video watch+quiz, pharmacist courses, CPD hour computation) were already fully built+verified in their own subsystems, this only removes the visibility flag. Untouched: unrelated `!auth.impersonating` spots (Create Quiz forms, Manage Staff, Join Quiz button, report forms) — different mechanism (Master impersonation view-only safety), not a feature flag. Orphaned `*.cpdComingSoon`/`dashboardView.cpdHoursComingSoon` i18n keys left in both locale files (unused now, harmless, not pruned — out of scope). Verified: `npm run build` clean (107 modules), EN/MS key-parity clean (822 keys/side), grep-confirmed zero leftover `disabled:true`/`ComingSoon` in the 6 edited files. **No live browser click-through** — no Playwright/browser tool available this session, same recurring gap as several prior subsystems. **Not pushed** — committed locally only pending user go-ahead, since pushing flips this live in production for real staff (Vercel auto-deploy on push to `master`). Recommend a manual click-through (real retail + pharmacist-tagged staff login) right after deploy, before telling staff to start using it.
[DONE, NOT PUSHED] Module Quiz CPD-hour cap + Area Manager assessment first-attempt fix — requested 2026-08-17, bounded (no spec/plan doc), brainstormed via clarifying questions before build. Two things bundled, confirmed with user: staff can retake Module Quiz topics as many times as they want (once/day, existing dedup unchanged), but only the first attempt per topic per calendar year should count toward CPD hours, and only the first-ever attempt should feed Area Manager's assessment report — retakes stay visible in staff/manager history only, never affect CPD total or the report. Video Training explicitly NOT capped (user confirmed retakes should keep stacking hours there) — same `results` table, split by topic membership in `video_trainings`, only the non-video branch changed. AI Practice CPD hour explored (Supervisor-settable rate) then explicitly dropped by user ("just continue 0.25h from before") — stays hardcoded flat 0.25hr/attempt, no admin UI, out of scope. 3 files: backend `routes/data.js`'s `cpdHoursThisYear()` — module-quiz branch changed from `sum(1hr per row)` to `count(distinct topic)` (mathematically equivalent to "first attempt only" since the rate is flat, no need to pick out which row is literally first); frontend `composables/useCpdHours.js`'s `hoursByStaff()` — same fix client-side, this is the function that feeds the 3 manager-tier CPD summary views (Outlet/Area Manager Results, Supervisor Staff Comparison) plus Quiz History's own CPD total, a `Set`-based per-(staff,topic) dedup added for the module-quiz branch only; `views/AreaManagerReviewsView.vue`'s `selectedResult` computed — **real pre-existing bug found and fixed**: was a plain `.find()` on a `created_at desc`-ordered `results` array, which silently picked the staff's LATEST retake instead of their first attempt, opposite of the intended assessment behavior; fixed to reduce to the earliest-Timestamp match. Not year-scoped (deliberate) — matches `reports.js`'s own all-time-once-per-outlet+staff+topic dedup, which has no year dimension, so scoping the attempt-pick to a year would've been meaningless (the report can only ever be filed once per topic regardless). Verified: backend fix tested live end-to-end against real prod Supabase via a disposable `TESTOUTLET|TESTSTAFF` staff JWT (minted via `issueToken`, no real PIN) hitting the real `GET /data/scoped-data` — 3 module-quiz attempts same topic (different days) + 1 different-topic attempt + 1 video-training topic with 2 attempts (2.5hr/video) returned `cpdHoursThisYear: 7` exactly as predicted (1+1 capped module, 5 stacked video), all test rows (`results`, disposable `video_trainings` row, `sessions` row) cleaned up after, confirmed zero leftover by count query. Frontend `hoursByStaff()` verified against the identical scenario with mock data via a standalone `node -e` import (same 7hr result, confirms client/server calcs stay in sync — memory had already flagged these two as a desync risk on any future rate-logic change, this was that moment). `AreaManagerReviewsView.vue`'s earliest-attempt reduce logic verified against mock out-of-order timestamps (correctly picked the middle-chronologically-earliest row, not array order). `npm run build` clean (107 modules) both before and after. **Known, not fixed (flagged, not asked for):** Quiz History's per-row "X CPD hour" label next to each history entry still shows the nominal module-quiz rate even on a now-uncounted retake row — cosmetic only, doesn't affect the summed total shown above it. **Not pushed** — committed locally only pending user go-ahead, same as the Coming-Soon-gate-removal commit above (`a757117`, already pushed) it builds on.
[PENDING] Module quiz question admin — discussed 2026-08-11, deferred. No in-app CRUD for `standard_questions` (Module Quiz) exists (`routes/questions.js` only has GET list + POST /:id/check grade); no admin UI either. User adding questions manually via Supabase table editor for now. video_questions (Video/Reading quiz) got its own CRUD 2026-08-17 (see entry above) — reuse that exact pattern here rather than a new shape. `correct` column confirmed 0-indexed (0=opt1...3=opt4) via live prod data + a live grading test, no longer an open question.
[PENDING] Outlet region reassign rule — user-stated 2026-08-11, not yet implemented/confirmed in code. When an outlet's area/region changes, Master can freely switch any outlet to the changed/new region — no restriction on which outlets can move to which area. Apply when building/reviewing `store_outlets.area_id` reassignment logic — don't lock an outlet to its original area.
[DONE, NOT PUSHED] CPD sub-project C: mandatory-course compliance report — requested 2026-08-17, full brainstorm/spec/plan/execute cycle, 7-task plan, all executed 2026-08-17 (last of the 3-part CPD initiative; A: CPD Hours Tracking and B: Pharmacist Tag + Gated Pharmacist Courses both done+pushed 2026-08-13, see their own entries above). Three bundled pieces, confirmed with user during brainstorming: (1) a pass/fail compliance matrix (pharmacist staff × mandatory `pharmacist_only` courses) on all 4 manager tiers — new `GET /pharmacist-compliance` (backend, scoped identically to `/data/scoped-data`, computed on read from `staff_roster`/`video_trainings`/`results`, no schema changes), new self-fetching `PharmacistComplianceMatrix.vue` dropped into `OutletManagerResultsView`/`WarehouseManagerResultsView`/`AreaManagerDashboard`/`SupervisorStaffComparisonView` (renders nothing when no pharmacist staff or no mandatory courses in scope — Warehouse Manager will always be empty since Pharmacist Courses are retail-only, kept anyway for tier parity). Compliance = best-ever attempt `percentage >= 70`, not most-recent — verified live via minted JWTs + throwaway `staff_roster`/`video_trainings`/`results` rows (no real PINs available this session): a 50% attempt followed by 80% then a lower 60% retake correctly stayed "passed" at 80%; Outlet/Area Manager scoping correctly excluded staff outside their outlet/region; staff-role tokens got 403. (2) A pharmacist-only "Download My Training Record" CSV button on Quiz History (topic/date/hours/running total, all-time not year-scoped) — the accepted mitigation for a known tradeoff: since the matrix is fully live off `results`, Annual Data Reset's purge (shipped, not pushed — see below) can silently revert a pharmacist to "not compliant" if their only passing attempt ages out; explicitly not solved by excluding rows from that purge. Gated behind both `auth.impersonating` (same boundary as the CPD data it's built from — real nav for Video Training/Pharmacist Courses still isn't flipped live) and `isPharmacist`. (3) Global CPD target lowered 120hr→60hr/year, unrelated to the report but bundled per explicit request — was a raw `CPD_TARGET_HOURS = 120` duplicated across 5 frontend view files with no shared source; centralized into one `export const CPD_TARGET_HOURS = 60` in `useCpdHours.js`, all 5 views + `PRODUCT.md` + the `videoHoursHelper` i18n string updated, grep-confirmed zero stray `120` references left (one stale doc-comment in `useCpdHours.js` referencing the old number was also caught and fixed). `npm run build` clean and EN/MS key-parity clean (no keys missing either direction) after every task. **Pushed 2026-08-17** — backend `081d75e` to `origin/main`, frontend `17ec7b5`..`83878a1` (7 commits) to `origin/master`, together with previously-unpushed Annual Data Reset (backend `c6cc5e6`, frontend `91705ea`/`865ad14` — user chose to push everything at once; nothing destructive ran, Annual Data Reset's real delete still requires Master to manually click through its own typed-DELETE gate).

**Live browser click-through done after push** (no Playwright MCP available, so driven via a local `playwright` script using the system-installed Chrome — `channel: 'chrome'` — plus minted JWTs and `localStorage` injection for all 4 manager tiers and an impersonated pharmacist staff session, same no-real-PINs pattern every prior subsystem used): Outlet/Area Manager/Supervisor all correctly showed the compliance matrix with real pass/fail cells (a 50%→80%→60% attempt sequence correctly displayed "Passed (80%)", confirming best-ever not most-recent live in the UI); a genuine warehouse-division outlet (`Warehouse`, not the retail-division `HQCT` first tried by mistake) correctly showed nothing. **Real bug caught by this click-through and fixed same session (commit `f401274`):** Task 5's `downloadTrainingRecord` i18n key was added to `supervisorStaffComparisonView`'s block instead of `quizHistoryView`'s — both blocks happen to declare identical section-heading keys (`videoTrainingHeading`, `cpdComingSoon`, etc.), so the EN/MS key-parity script stayed clean (both locale files still had matching key sets, just under the wrong parent) and the mistake wasn't caught until the download button rendered its own literal key text in the browser. Fixed by moving the key to the correct block in both locale files; re-verified live — button now shows "Download My Training Record", downloads a correct CSV (`Topic,Date,Hours` rows + `Total` footer, e.g. `2.25` total hours matching the on-screen CPD number). Spec: `docs/superpowers/specs/2026-08-17-cpd-compliance-report-design.md`. Plan: `docs/superpowers/plans/2026-08-17-cpd-compliance-report.md`.
[DONE, PUSHED] Video Training — requested 2026-08-12, brainstorm/spec/plan/execute cycle, 11 tasks, all done+verified 2026-08-12/13, pushed to origin. Retail staff watch a YouTube-embedded (Unlisted) video in-app; on real `ENDED` event, routes into `/quiz` (reuses `QuizView.vue`, new `kind: 'video'`) with a 30s-per-question timer, drawn from a new separate `video_questions` bank (same shape as `standard_questions`, never mixed). Graded+saved into the same `results` table as Module Quiz. Anti-fraud abandon-lock widened to also cover this kind. New backend tables `video_trainings`/`video_questions` + routes mirroring `content.js`/`questions.js`/`POST /data/results` one-for-one. Supervisor-only management (`SupervisorAddResourcesView.vue`), retail-only nav (same gate as Module Quiz). **Accepted risk, explicitly discussed:** YouTube Unlisted is not access-controlled — Private/Drive+Workspace/Vimeo alternatives all considered and rejected, free YouTube Unlisted chosen knowingly. **Task 11 (added mid-build, 2026-08-13):** the "Video Training" nav item is gated behind `auth.impersonating` — real staff see a greyed "Coming Soon" chip (non-clickable), Master-impersonated sessions see the real feature. Not yet flipped live for real staff — remove the `auth.impersonating` condition in `AppSidebar.vue` when ready. Spec: `docs/superpowers/specs/2026-08-12-video-training-design.md`. Plan: `docs/superpowers/plans/2026-08-12-video-training.md`. Verified: full curl round-trips + `npm run build` clean per backend task, live browser click-through per frontend task (Playwright) including a real end-to-end pass (countdown, live reveal, timeout auto-advance, submit→Result, Quiz History, Back-disabled, abandon-lock confirm dialog) and both Coming-Soon states. **2 real bugs found+fixed during build (not in the original plan text, caught live):** (1) automated security review flagged `extractYouTubeId()`'s unanchored regex substring match in `routes/videoTraining.js` — rewrote to parse as a real URL and check hostname exactly (commit `c42e91d`); same fix mirrored in `VideoWatchView.vue`'s client-side copy for consistency. (2) Task 11's disabled-nav click handler `@click="item.disabled ? null : navigate"` — a ternary, which Vue evaluates as an inline expression rather than invoking as a handler, so it silently broke **every** sidebar nav item's click-to-navigate (not just the gated one) until caught by live-browser verification and fixed to `navigate()` (commit `fa4bb7d`). Backend pushed `287248c..c42e91d` (5 commits), frontend pushed `6b077db..fa4bb7d` (12 commits, includes the CPD revision spec/plan commits below).
[DONE, NOT PUSHED] CPD Hours Tracking (120hr/year) — requested 2026-08-12, revised 2026-08-13, executed+verified 2026-08-13 (all 10 plan tasks). Sub-project A of a 3-part CPD initiative (B: `is_pharmacist` tag + gated nav section — done, see entry below; C: compliance report — still unspec'd, not started). Three sources: Video Training (Supervisor-set `video_trainings.hours`, numeric, default 1), Module Quiz (flat 1hr/attempt), AI Practice (flat 0.25hr/attempt) — flat rates chosen because neither has a fixed per-entry admin row to attach hours to. No new ledger table — hours-this-year always computed on read (backend: `cpdHoursThisYear()` helper in `data.js`, joins `results`→`video_trainings` by topic + sums `ai_results`, filtered to current calendar year; frontend: `useCpdHours.js` composable, same join logic client-side for the 3 manager-facing per-staff breakdowns). **Real gap found+fixed during Task 4:** Area Manager's `GET /scoped-data` branch had hardcoded `aiResults: []` since long before this feature — fixed so Area Manager's CPD summary (and any future AI Practice display there) gets real data; verified live in browser that MOHD HAFIZ's total correctly includes AI Practice hours after the fix. Coming Soon gating (`auth.impersonating`, same pattern as Video Training's Task 11) on Dashboard's progress line + all 3 manager summary sections — confirmed live for every one of the 5 gated UI spots: real login shows "Coming soon", Master-impersonated session shows the real number, using minted JWTs injected into `localStorage` (no real manager/supervisor PINs were available this session) rather than real-credential login — same `lautan_token`/`lautan_manager`/`lautan_staff`/`lautan_impersonating` keys `store/auth.js` already uses. Supervisor Staff Comparison's CPD summary deliberately uses a separate always-current-year `getScopedData(0)` fetch (not the windowMonths-scoped one) — verified live that switching the "Last 3 months" dropdown does NOT change the CPD numbers, confirming the two data sources are properly decoupled. Video-hours form field: HTML5 `min="0.5"` blocks literal `0` natively (browser's own validation message, confirmed live); the JS `Number.isFinite`/`<=0` check is the fallback for the empty/non-numeric case HTML5 alone doesn't catch (also confirmed live — emptying the field surfaces "Hours must be a positive number."). EN/MS key parity clean (718 keys/side) after all edits. Backend commits `86876ce`, `f736c37`, `ba9ac74`, `e295570`. Frontend commits `db59e3a`, `9a44d9c`, `c167bb5`, `16e5a0a`, `d7f8e08`, `027e1e6` — **note: i18n keys for Tasks 7-10 all landed bundled into the `9a44d9c` Dashboard commit** (all i18n edits were made across the two locale files before the first commit in this run), so those 4 commits are code-only despite each task's own plan text listing locale files as "modified" — harmless (end state matches the plan exactly, EN/MS parity confirmed), just less granular than the plan's per-task commit boundaries. Revision spec: `docs/superpowers/specs/2026-08-13-cpd-hours-revision-design.md`. Plan: `docs/superpowers/plans/2026-08-12-cpd-hours.md`. **Pushed 2026-08-13** (backend `86876ce..e295570`, frontend `db59e3a..027e1e6`) together with the Results Filters feature below — see that entry for push details.
[DONE, PUSHED] Results Filters & Video/Module/AI Source-Split Sections — requested 2026-08-13, brainstorm/spec/plan/execute cycle (subagent-driven, 5 tasks), all executed+reviewed+fixed 2026-08-13. Splits every merged "Video Training + Module Quiz" list (same `results` table, distinguished only by topic membership in `video_trainings`) into two real sections, adds AI Practice as its own section in Area Manager (data existed since the CPD Hours backend fix but was never rendered), standardizes year/topic/staff-name filtering, makes every CPD Hours summary year-filterable instead of hardcoded to the current year. New `splitByVideoTopic(results, hoursByTopic)` export on `useCpdHours.js`, reused by all 4 views. Outlet Manager gets a staff-name filter on all 3 quiz sections (per explicit ask, not extended to Area Manager/Supervisor). Supervisor keeps its existing `windowMonths` dropdown alongside new per-section year/topic/sort (3 independent leaderboards via a new local `buildLeaderboard()` helper, not a shared composable — matches spec's explicit YAGNI call). Staff Quiz History gets a brand-new CPD Hours section (previously only the Dashboard had one) plus year-only filters (no topic — a single staff's own topic list per source is short). **Real bug caught in Task 2's review, fixed proactively across Tasks 2-4:** every CPD section was originally gated `v-if="auth.impersonating && cpdSummary.length"` — picking a year with zero data hid the ENTIRE section including the year `<select>` itself, no way back except a page reload; fixed to gate the section on `auth.impersonating` alone with an inner list/empty-state split (Task 5's CPD section shows one staff's single value, not a list, so it was never affected). **Final whole-branch review found 6 Important + 6 Minor findings no single task's reviewer could see**, all fixed in one batch + re-reviewed clean: a `Promise.all` in Quiz History that let a failing `/video-trainings` call blank the entire page (results/AI/wrong-answers/assessment reports all wiped, not just Video Training) — now `.catch()`-guarded; Area Manager's page-level empty guard hid the new AI Practice/CPD sections for a region with AI-Practice-only data — guard extended; Supervisor was missing the filter-reset-on-scope-change watcher Area Manager already had (9 refs, 3 scope controls) — added; `ms.json` had diverged Malay wording for the same section name across views ("Modul Kuiz" vs "Kuiz Modul", "Amalan AI" vs "Latihan AI") — aligned; Quiz History's CPD computed used `[0]?.hours` which silently undercounts a staff member with rows under 2 outlets (e.g. after a transfer) — changed to sum all groups; noted (comment only, not restructured) that Dashboard's CPD number is server-computed and Quiz History's is client-computed — two sources of truth for the same label, currently in agreement but could desync if the backend rate logic changes. All verification (build clean + EN/MS parity clean, 22 new keys) done per-task by subagents in sandboxed environments without dev server/DB access — **no live browser click-through has happened yet for this feature**, unlike CPD Hours Tracking above which got a full Playwright pass. Design spec: `docs/superpowers/specs/2026-08-13-results-filters-sections-design.md`. Plan: `docs/superpowers/plans/2026-08-13-results-filters-sections.md`. Frontend commits `975cffb`..`a31fda6` (11 commits, includes 2 plan-fix commits mixed in — `06990cd` fixed the plan text for the CPD bug before Tasks 3/4 were dispatched). **Pushed 2026-08-13 without live browser verification** — user chose to push now rather than wait for a click-through; backend `86876ce..e295570` (4 commits, CPD Hours Tracking's backend half, pushed together), frontend `db59e3a..058f738` (pushed to `origin/master`). **Still needed:** live click-through, especially the final review's highest-risk unknown — confirm a warehouse-division staff token can actually call `GET /video-trainings` without a 403/500 (Quiz History now fetches it for every staff member regardless of division, guarded by `.catch()` so a failure degrades gracefully rather than blanking the page, but the real backend response was never observed).

[DONE, PUSHED] Pharmacist Tag + Gated Pharmacist Courses (CPD sub-project B) — requested 2026-08-13, full brainstorm/spec/plan/execute cycle (inline, 10-task plan + 1 mid-build addition), executed+verified 2026-08-13. `staff_roster.is_pharmacist` (Supervisor-only tag, UI label "Pharmacist" — column name unchanged), set from a new company-wide staff directory tab (`SupervisorPharmacistTagView.vue`, outlet-filterable) — no such company-wide staff list existed before this. `video_trainings` extended with `kind` ('video'|'reading'), `pharmacist_only`, `body` (reading text) — `youtube_url` made nullable — so Pharmacist Courses reuses the same table/hours-crediting mechanism as general Video Training instead of a new one; both count toward the same 120hr CPD target with no new accounting. New `GET /video-trainings/pharmacist` (staff: live `is_pharmacist` DB check every request, not the client's login-time flag, which is advisory-only for nav visibility; Supervisor: unconditional read access for its own management view). `GET /video-trainings` extended to exclude `pharmacist_only` rows so the general list never shows them to anyone. New staff-facing `PharmacistCoursesListView.vue` + `ReadingView.vue` (reading-kind's "I've read this" button replaces the video-kind's YouTube `ENDED` event as the quiz-unlock gate; same `sessionStorage` handoff into `QuizView.vue`, still `kind: 'video'` in that envelope since grading/hours only key off topic). **Nav follows Video Training's existing not-yet-flipped-live gate** (`auth.impersonating`) — Pharmacist Courses reuses that same watch/quiz infrastructure, so it stays a greyed "Coming Soon" for real staff until Video Training itself is flipped live; both should be un-gated together. **Real bug caught and fixed during Task 8, before real bugs could reach users:** `VideoWatchView.vue` looked up its video by id from `GET /video-trainings` alone — since that now excludes `pharmacist_only` rows, a pharmacist-only *video* course (not reading) would 404 on its own watch page; fixed with a fallback fetch to `GET /video-trainings/pharmacist` when the general list doesn't have the id. **Real gap found during Task 7's live verification, fixed in a follow-up round (user-approved) before considering the feature done:** Supervisor could create pharmacist-only courses via the add-course form but the same page's own list only read the general (now-filtered) endpoint, so they became invisible/undeletable immediately after creation with no UI path back — fixed by having `SupervisorAddResourcesView.vue` merge both endpoints (tagged with a "Pharmacist only" badge) and extending `GET /video-trainings/pharmacist`'s auth to also allow the `supervisor` scope type unconditionally (not just tagged staff). User also asked for the outlet filter on the tag directory in the same round. Verified: full curl round-trips per backend task (401/403/404/400 cases, audit-log rows, `staff_roster.is_pharmacist` read-back) using disposable/throwaway edits to real production rows (tagged and untagged back, test courses/questions created and deleted, one disposable staff account created and deleted for a login-response test since no real staff PIN was available) — same caution as every prior subsystem; `npm run build` clean and EN/MS key-parity clean (764 keys/side final) after every frontend task; live browser click-through (Playwright, minted JWTs injected into `localStorage` for both Supervisor and impersonated-staff sessions since no real PINs were available this session, same pattern as CPD Hours Tracking) covering: tag toggle + persistence across reload, outlet filter, add-course form's kind/body/checkbox conditionals, gated nav absent for non-tagged/present for tagged, direct-URL 403 for non-tagged staff, pharmacist-only video course's watch-page fallback fix, full reading→"I've read this"→real quiz→live answer-check pass (correctly blocked by the pre-existing impersonation view-only guard, confirming no new gap there). Spec: `docs/superpowers/specs/2026-08-13-pharmacist-tag-design.md`. Plan: `docs/superpowers/plans/2026-08-13-pharmacist-tag.md`. **Follow-up fix same session:** mobile bottom nav was a single-row flex dividing all items equally — Supervisor's new 8th tab (Pharmacist) got pushed off-screen entirely and every label truncated; changed to a 4-column grid that wraps into 2 rows (commit `9e45e1e`), verified in a 390px viewport (Playwright) that all 8 items render with zero label overflow. Backend commits `0db00f6`..`92ef2bc` (5 commits). Frontend commits `ace5339`..`9e45e1e` (7 commits, plus spec/plan commits `a7ed487`/`73b21ea`). **Pushed 2026-08-13** — backend `e295570..92ef2bc` to `origin/main`, frontend `f8944c9..9e45e1e` to `origin/master`.

[FIXED] Module Quiz Submit dead-end (2026-08-14, user-reported via screenshot: "cannot submit module quiz, even after refresh"). Root cause in `QuizView.vue`: Module Quiz/Video Training lock the Back button permanently once any question is answered (anti-cheat, existing behavior) — but Next had no requirement that the current question be answered first. If a single question's live check-call failed (network blip, or the 80-req/10min rate limit in `questions.js`'s `POST /:id/check`), `selectAnswer()` silently left that question unanswered; nothing stopped tapping Next past it. Reaching the last question, answering it (matches the screenshot — correct answer, checkmark shown), still left `answeredCount < questions.length`, so Submit stayed disabled forever with zero explanation, and Back was already locked — a genuine dead end, reproducible every retry since `answers` isn't persisted to `sessionStorage` (refresh restarts from Q1 but hits the same trap if the same question fails again). AI Practice unaffected — Back isn't locked there, so a skipped question is recoverable. Fix: new `canManuallyAdvance` computed disables the Next button until the current question is answered — closes the skip path at the source instead of adding an error message. Extended to AI Practice too, same session, per explicit user ask ("ai practice also need the logic") — AI Practice has no Back-lock so wasn't a dead end, but now requires an answer before advancing for consistency across all 3 kinds. Deliberately does NOT touch `next()`/`submitQuiz()` themselves — Video Training's 30s-per-question timer calls those directly on timeout to allow blank auto-advance, which must keep working. `npm run build` clean. **Not yet live-tested in browser** (no backend/Supabase credentials in this session) — recommend a real Module Quiz attempt end-to-end before/after pushing.

[FIXED] Area Manager Assessment staff picker (2026-08-12, commit `428929c`) — `GET /auth/staff-roster` was changed to return `{name, idNote}` objects (idNote disambiguation work, 2026-08-10/11) but `AreaManagerReviewsView.vue`'s staff `<select>` was missed, still bound `:value` to the whole object. Broke topic/result matching (`r.Name === formStaff.value` never matched) and rendered raw JSON in the dropdown. Fixed to match `LoginView.vue`'s existing `n.name`/`n.idNote` pattern — check any other `api.getStaffNames` call site the same way if this class of bug resurfaces.

[DONE, PUSHED] Module Quiz anti-fraud abandon lock — requested 2026-08-12, full brainstorm/spec/plan/execute cycle, 5 tasks, commits `50b5a5f`..`603bd33` (spec `6a752c6`, plan `b81b882`), pushed to origin/master. Standard/Module Quiz only (`kind === 'standard'` in `QuizView.vue`) — AI Practice untouched. Problem: staff could answer Q1, see it's wrong, abandon before Submit, and retry fresh since `results` rows were only written from the Submit button. Fix: `submit()` split into `gradeAndSave()` (pure grade+save, reusable) + `submitQuiz()` (button path: grade+save then navigate to Result); a `hasSubmitted` guard makes all paths mutually exclusive. Three ways an attempt now gets recorded: (1) normal Submit button, (2) `onBeforeRouteLeave` guard — in-app nav away after >=1 answer triggers a `confirm()` warning, then grades+saves on OK, blocks on Cancel, (3) `pagehide` listener (real tab/app close) — best-effort `fetch(..., {keepalive:true})` via new `api.saveResultKeepalive`, no dialog possible. Back button disabled once `answeredCount >= 1` (Module Quiz only). **Deliberate deviation from the written spec:** close-detection uses `pagehide` only, NOT `visibilitychange` — the spec's wording listed both, but `visibilitychange` fires on any backgrounding (phone call, app-switch), which would wrongly finalize/lock an attempt every time staff get interrupted mid-quiz on the shop floor; `pagehide` matches real exit/close instead, at the cost of the already-accepted force-kill-before-pagehide-fires edge case. No backend/schema changes — reuses `POST /data/results` as-is (already grades missing `chosen` as wrong, already no-ops a same-day duplicate). `saveResultKeepalive` in `api/client.js` bypasses `request()`/`sendBeacon` because `sendBeacon` can't carry the `Authorization` header this API needs. User confirmed live in browser 2026-08-12 — working. Spec: `docs/superpowers/specs/2026-08-12-module-quiz-anti-fraud-design.md`. Plan: `docs/superpowers/plans/2026-08-12-module-quiz-anti-fraud.md`.
[PENDING] Task 5's `fetch keepalive` iOS gap — discussed 2026-08-12, user: leave as-is for now. Safari only supports the `keepalive` fetch flag from Safari/iOS 16.4 (March 2023) onward; older iPhones (iOS 14.0-16.3) silently lose the tab/app-close abandon-catch (Task 4's in-app nav-away guard still works everywhere, unaffected — pure JS, no special API). If revisited: would need a `sendBeacon` fallback for unsupported browsers, which can't carry the `Authorization` header, so also needs a small backend change to accept it.

[DONE, PUSHED] Pagination rollout — requested 2026-08-14 ("pagination not working"). Root cause: pagination never existed anywhere in the frontend, not a regression — every list rendered its full array via unbounded `v-for` (confirmed by grep for pagination/LIMIT/OFFSET across `src/`, zero hits). New shared `composables/usePagination.js` (client-side, 15/page default, resets to page 1 when source array changes so a filter change can't strand you past the last page) + `components/Pagination.vue` (prev/next + page indicator, i18n'd via new `pagination.*` keys in both locale files). Wired into ~34 list sites across 21 files spanning all 5 roles — Master (`MasterAuditLog`, `MasterActiveSessions`, `MasterImpersonation`, all 4 Purge panels), Area Manager (`AreaManagerDashboard` ×4, `AreaManagerReviewsView`), Outlet/Warehouse Manager (`ManageStaffPanel`, `OutletManagerResultsView` ×4, `OutletManagerStaffReviewView`, `WarehouseManagerResultsView`), Supervisor (`SupervisorPharmacistTagView`, `SupervisorStaffComparisonView` ×4, `SupervisorReportsView`, `SupervisorAddResourcesView` ×2, `SupervisorDashboard`), Staff (`QuizHistoryView` ×4, `VideoTrainingListView`, `PharmacistCoursesListView`, `ResourcesView`). **`SupervisorDashboard.vue`'s activity log:** replaced its old hardcoded `.slice(0, 100)` + `max-h-[32rem] overflow-y-auto` scroll cap with real pagination — a deliberate behavior change (older activity beyond the old 100-row cutoff is now reachable), not just a like-for-like swap. **Known gap, not built:** `MasterOutletsPanel.vue`'s two lists (outlets-by-area, warehouse outlets) skipped — each area's outlet sublist is a separate dynamic per-row list, doesn't fit the single-list composable without bespoke per-area pagination state; current scale (~50 outlets / 9 areas ≈ 5-6/area) is well under 15 so low priority, flagged to user rather than silently dropped. Verified: `npm run build` clean (105 modules, no errors) after all edits; grep-confirmed no orphaned `paginated*` template references across all 21 edited files. **No live browser click-through** — no backend/Supabase credentials available in this session's environment to run the real app end-to-end; recommend a manual spot-check (any list with 16+ real rows, e.g. Master Audit Log or the company-wide Staff Comparison) before/after pushing.

[DONE, NOT PUSHED] Annual Data Reset — requested 2026-08-14 (Supabase free-tier egress/storage concern discussed: 500MB DB / 1GB file storage / 5GB egress monthly caps), full brainstorm/spec/plan/execute cycle, 4-task plan, all executed+verified 2026-08-14. Master-only: archive-then-clear quiz-attempt history + audit log dated before Jan 1 of the current calendar year, from the existing Backup & Export panel. New `routes/masterAnnualReset.js` (`GET /master/annual-reset/preview` read-only counts, `POST /master/annual-reset` transactional delete), covers exactly 6 tables (`results`/`wrong_answers`/`ai_results`/`ai_wrong_answers`/`reports`/`audit_log`) — no FK constraints between them, confirmed against `sql/schema.sql`, so delete order doesn't matter. Staff/manager accounts, outlets/areas, content, question banks all untouched — nobody re-registers. **Confirmed non-dependency:** CPD Hours already only ever computes the current calendar year live from the DB (`cpdHoursThisYear()` in `data.js`), so this reset changes zero CPD numbers, purely a storage/egress cleanup. Reset button is **hard-gated**: `MasterBackupExport.vue`'s new `backedUp` ref only flips true after a real export succeeds in that panel visit, resets on every fresh mount (not persisted) — reuses the existing `MasterDeleteConfirmModal` typed-DELETE gate, same as every Purge panel. The reset's own `audit_log` row is written inside the same transaction, dated `now()`, so it survives the very cutoff it describes (verified live). **Real safety call made mid-build:** this backend's `.env` points at the real production Supabase DB — no separate test/staging DB exists for this project (known gap, see the Automated Test Suite PENDING entry above). Every other Purge endpoint's curl verification only ever touches specifically-tagged test rows, but this endpoint deletes by date cutoff across all 6 tables company-wide with no id filter — actually calling `POST /master/annual-reset` to "test" it would have been a real, irreversible, company-wide delete with no real backup taken first. **Deliberately did not execute the destructive path this session** — verified only the safe read-only surface against real data instead: `GET /preview` returns the correct cutoff (`2026-01-01T00:00:00.000Z`) and correct real counts (all `0` — no rows predate 2026 yet, app went live this year), 401 for no/garbage token, 403 for a valid-but-non-master token (minted via `issueMasterToken`/`issueToken` directly, no real credentials needed — `requireAuth` explicitly skips the sessions-table revocation check for `scopeType==='master'`, confirmed in `middleware/auth.js`), throwaway test session row cleaned up after. `npm run build` clean after every frontend task. **No live browser click-through** — no browser automation tool available in this session despite CLAUDE.md listing Playwright MCP; the UI wiring (gate logic, preview rendering, modal reuse) is code-reviewed and build-clean but not clicked through live. Spec: `docs/superpowers/specs/2026-08-14-annual-data-reset-design.md`. Plan: `docs/superpowers/plans/2026-08-14-annual-data-reset.md`. **Not pushed** — backend `c6cc5e6`, frontend `91705ea`/`865ad14` committed locally only. **Before first real use:** export a real backup, then live-browser-verify the gate + modal flow, before ever clicking the real Reset button for the first time.
