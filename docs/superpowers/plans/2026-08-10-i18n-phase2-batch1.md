# i18n Phase 2 — Batch 1 (staff quiz flow) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrate the 6 staff-facing quiz-flow views (`DashboardView.vue`, `ModuleQuizView.vue`, `QuizView.vue`, `ResultView.vue`, `QuizHistoryView.vue`, `ResourcesView.vue`) to `vue-i18n`, with real EN and BM text for every string in scope — no placeholders. Also reconciles `QuizView.vue`'s pre-existing local `lang` toggle onto the shared `vue-i18n` mechanism (the "open risk" flagged in the Phase 1 design spec).

**Architecture:** Same as Phase 1 — `useI18n()` (Composition API) per component, one top-level namespace per view added to the existing `src/i18n/locales/{en,ms}.json`. `QuizView.vue` currently drives which language a question's `_en`/`_ms` fields show via a local `lang` ref with its own inline toggle button — this plan replaces that with the app-wide `locale` from `useI18n()` and the shared `<LanguageSwitcher />` component, so question-content language and UI-chrome language become the same single switch instead of two independent ones.

**Tech Stack:** Vue 3 (Composition API, `<script setup>`), `vue-i18n` (already installed/registered — Phase 1), existing Tailwind tokens. No test runner exists in this project — verification is `npm run build` plus manual click-through.

## Global Constraints

- No new libraries (CLAUDE.md: no new frameworks/libs without asking — `vue-i18n` already approved in Phase 1, nothing new needed here).
- BM text is authored directly (not machine-translated via a paid API) — spot-check by a native speaker recommended afterward, per Phase 1 spec's open risk note, still unresolved.
- Match existing file conventions: `<script setup>`, Tailwind utility classes inline, no scoped CSS beyond existing `@keyframes rise` blocks where present.
- Bilingual EN/MS parity: every key added to `en.json` must have a matching key in `ms.json` in the same task — never land a task with an English-only key.
- Brand name "Lautan Academy" is never translated.
- Dynamic/DB-sourced values (staff names, outlet codes, topic names, report free-text fields, dates as rendered by `toLocaleDateString`) are **not** translated — only static UI chrome (labels, buttons, headings, error messages) gets a translation key.
- Reuse existing keys instead of duplicating where the exact same concept already has one — e.g. `ResourcesView.vue`'s manager-role header label reuses Phase 1's `sidebar.roleOutletManager` etc. rather than adding a second copy of the same 4 strings.
- `vue-i18n`'s built-in pluralization (`t('key', count)` with a `|`-separated message) is used for English "1 thing" vs "2 things" phrasing. Bahasa Melayu doesn't inflect nouns for plural, so `ms.json` entries for these keys stay a single form (no `|`) — vue-i18n falls back to that one string regardless of count, which is correct BM grammar, not a missing translation.

---

### Task 1: Migrate DashboardView.vue

**Files:**
- Modify: `lautan-academy-frontend/src/views/DashboardView.vue`
- Modify: `lautan-academy-frontend/src/i18n/locales/en.json` (add `dashboardView` key)
- Modify: `lautan-academy-frontend/src/i18n/locales/ms.json` (add `dashboardView` key)

**Interfaces:**
- Consumes: `useI18n()` from `'vue-i18n'` (already registered, Phase 1 Task 1).

- [ ] **Step 1: Add `dashboardView` to en.json**

Merge as a new top-level key into `lautan-academy-frontend/src/i18n/locales/en.json`, alongside the existing `loginView`/`managerLogin`/etc. keys (don't overwrite the file):

```json
"dashboardView": {
  "greeting": "Hi {name}",
  "progressLabel": "Your Progress",
  "noPracticeYet": "No practice yet",
  "averagingPercent": "Averaging {percent}%",
  "joinCodeBelow": "Join a code below to get started",
  "practiceAttempts": "{count} practice attempt so far | {count} practice attempts so far",
  "joinQuizBtn": "Join a Quiz",
  "browseCourses": "Browse Courses",
  "viewAll": "View all",
  "noCourseMaterial": "No course material uploaded yet.",
  "materialsCount": "{count} material | {count} materials",
  "recentPractice": "Recent Practice",
  "noPracticeAttempts": "No practice attempts yet.",
  "joinPracticeQuiz": "Join a Practice Quiz",
  "joining": "Joining...",
  "join": "Join",
  "errorBadCode": "Enter the 3-digit practice code your manager shared.",
  "errorInvalidCode": "Invalid or expired code for your outlet."
}
```

- [ ] **Step 2: Add matching `dashboardView` to ms.json**

```json
"dashboardView": {
  "greeting": "Hai {name}",
  "progressLabel": "Kemajuan Anda",
  "noPracticeYet": "Belum ada latihan",
  "averagingPercent": "Purata {percent}%",
  "joinCodeBelow": "Sertai kod di bawah untuk mula",
  "practiceAttempts": "{count} percubaan latihan setakat ini",
  "joinQuizBtn": "Sertai Kuiz",
  "browseCourses": "Semak Kursus",
  "viewAll": "Lihat semua",
  "noCourseMaterial": "Belum ada bahan kursus dimuat naik.",
  "materialsCount": "{count} bahan",
  "recentPractice": "Latihan Terkini",
  "noPracticeAttempts": "Belum ada percubaan latihan.",
  "joinPracticeQuiz": "Sertai Kuiz Latihan",
  "joining": "Menyertai...",
  "join": "Sertai",
  "errorBadCode": "Masukkan kod latihan 3-digit yang dikongsi pengurus anda.",
  "errorInvalidCode": "Kod tidak sah atau luput untuk cawangan anda."
}
```

- [ ] **Step 3: Update the script block**

In `lautan-academy-frontend/src/views/DashboardView.vue`, add the import and `t`:

```js
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useI18n } from 'vue-i18n'
import { api } from '../api/client'
import { useAuthStore } from '../store/auth'
import ProgressRing from '../components/ProgressRing.vue'
import DigitCode from '../components/DigitCode.vue'

const { t } = useI18n()
```

(insert `import { useI18n } from 'vue-i18n'` after the `vue-router` import, and `const { t } = useI18n()` as the first line after the existing imports, before `const passcode = ref('')`.)

Replace the two hardcoded error strings inside `joinQuiz()`:

```js
async function joinQuiz() {
  joinError.value = ''
  if (!/^\d{3}$/.test(passcode.value)) {
    joinError.value = t('dashboardView.errorBadCode')
    return
  }
  joining.value = true
  try {
    const data = await api.redeemAiQuiz(auth.staff.outlet, passcode.value)
    if (!data.questions || !data.questions.length) throw new Error(data.error || 'empty')
    sessionStorage.setItem('lautan_active_quiz', JSON.stringify({ topic: data.topic, questions: data.questions, passcode: data.passcode }))
    router.push('/quiz')
  } catch (err) {
    joinError.value = t('dashboardView.errorInvalidCode')
  } finally {
    joining.value = false
  }
}
```

- [ ] **Step 4: Update the template**

Replace each of the following in-place (leave everything else — layout, classes, `v-if`/`v-for`, dynamic data bindings like `c.name`/`h.Topic` — untouched):

```html
<h1 class="font-display text-2xl font-semibold text-ink mt-0.5 truncate">{{ t('dashboardView.greeting', { name: firstName }) }}</h1>
```

```html
<p class="text-aqualight text-[11px] font-semibold uppercase tracking-wide">{{ t('dashboardView.progressLabel') }}</p>
<p class="font-display text-lg md:text-xl font-semibold text-white mt-1">
  {{ history.length === 0 ? t('dashboardView.noPracticeYet') : t('dashboardView.averagingPercent', { percent: avgPercent }) }}
</p>
<p class="text-white/60 text-sm mt-0.5">
  {{ history.length === 0 ? t('dashboardView.joinCodeBelow') : t('dashboardView.practiceAttempts', history.length) }}
</p>
<button type="button" @click="scrollToJoin" class="mt-4 bg-aqua text-white text-sm font-medium px-5 py-2.5 rounded-lg hover:opacity-90 transition-opacity">
  {{ t('dashboardView.joinQuizBtn') }}
</button>
```

```html
<h2 class="font-display text-base font-semibold text-ink">{{ t('dashboardView.browseCourses') }}</h2>
<RouterLink to="/resources" class="text-aqua text-sm font-medium shrink-0">{{ t('dashboardView.viewAll') }}</RouterLink>
```

```html
<div v-else-if="categoryCards.length === 0" class="bg-white rounded-xl2 p-6 text-center text-slate text-sm">
  {{ t('dashboardView.noCourseMaterial') }}
</div>
```

```html
<p class="text-xs text-slate mt-0.5">{{ t('dashboardView.materialsCount', c.count) }}</p>
```

```html
<h2 class="font-display text-sm font-semibold text-ink mb-3">{{ t('dashboardView.recentPractice') }}</h2>
```

```html
<div v-else-if="recentPractice.length === 0" class="text-slate text-xs">{{ t('dashboardView.noPracticeAttempts') }}</div>
```

```html
<h2 class="font-display text-base font-semibold text-ink mb-3">{{ t('dashboardView.joinPracticeQuiz') }}</h2>
```

```html
<button type="submit" :disabled="joining" class="bg-coral text-white font-medium px-6 py-3 rounded-lg disabled:opacity-60 hover:opacity-90 transition-opacity">
  {{ joining ? t('dashboardView.joining') : t('dashboardView.join') }}
</button>
```

- [ ] **Step 5: Verify**

```bash
npm run build
```
Expected: clean build, no errors. Manually load `/` (staff dashboard) logged in as a real/test staff account, toggle language from the sidebar switcher, confirm every label above switches — including the two dynamic-count strings (average score card, material count on at least one Browse Courses card) and both empty-state variants if reachable (a fresh/no-history account).

- [ ] **Step 6: Commit**

```bash
git add lautan-academy-frontend/src/views/DashboardView.vue lautan-academy-frontend/src/i18n/locales/en.json lautan-academy-frontend/src/i18n/locales/ms.json
git commit -m "feat(i18n): migrate DashboardView to vue-i18n"
```

---

### Task 2: Migrate ModuleQuizView.vue

**Files:**
- Modify: `lautan-academy-frontend/src/views/ModuleQuizView.vue`
- Modify: `lautan-academy-frontend/src/i18n/locales/en.json` (add `moduleQuizView` key)
- Modify: `lautan-academy-frontend/src/i18n/locales/ms.json` (add `moduleQuizView` key)

**Interfaces:**
- Consumes: `useI18n()` from `'vue-i18n'`.

- [ ] **Step 1: Add `moduleQuizView` to en.json**

```json
"moduleQuizView": {
  "title": "Module Quiz",
  "loading": "Loading...",
  "moduleLabel": "Module",
  "selectModule": "Select a module...",
  "questionCount": "{count} question in this module. | {count} questions in this module.",
  "errorPickModule": "Pick a module first.",
  "errorAlreadyAttempted": "You've already attempted \"{topic}\" today. Try again tomorrow.",
  "errorNoQuestions": "No questions available for this module yet.",
  "startQuiz": "Start quiz"
}
```

- [ ] **Step 2: Add matching `moduleQuizView` to ms.json**

```json
"moduleQuizView": {
  "title": "Kuiz Modul",
  "loading": "Memuatkan...",
  "moduleLabel": "Modul",
  "selectModule": "Pilih modul...",
  "questionCount": "{count} soalan dalam modul ini.",
  "errorPickModule": "Pilih modul dahulu.",
  "errorAlreadyAttempted": "Anda telah mencuba \"{topic}\" hari ini. Cuba lagi esok.",
  "errorNoQuestions": "Tiada soalan tersedia untuk modul ini lagi.",
  "startQuiz": "Mula kuiz"
}
```

- [ ] **Step 3: Update the script block**

Add the import/`t`, and use it in `start()`:

```js
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useI18n } from 'vue-i18n'
import { useAuthStore } from '../store/auth'
import { api } from '../api/client'

const router = useRouter()
const auth = useAuthStore()
const { t } = useI18n()
```

```js
function start() {
  error.value = ''
  if (!selectedTopic.value) { error.value = t('moduleQuizView.errorPickModule'); return }
  if (alreadyAttemptedToday.value) { error.value = t('moduleQuizView.errorAlreadyAttempted', { topic: selectedTopic.value }); return }
  const questions = shuffle(allQuestions.value.filter((q) => q.topic === selectedTopic.value))
  if (!questions.length) { error.value = t('moduleQuizView.errorNoQuestions'); return }
  sessionStorage.setItem('lautan_active_quiz', JSON.stringify({ kind: 'standard', topic: selectedTopic.value, questions }))
  router.push('/quiz')
}
```

- [ ] **Step 4: Update the template**

```html
<h1 class="font-display text-xl font-semibold text-white">{{ t('moduleQuizView.title') }}</h1>
```

```html
<div v-if="loading" class="text-slate text-sm">{{ t('moduleQuizView.loading') }}</div>
<div v-else class="bg-white rounded-xl2 p-6 shadow-sm space-y-4">
  <div>
    <label class="block text-sm font-medium text-ink mb-1">{{ t('moduleQuizView.moduleLabel') }}</label>
    <select v-model="selectedTopic" @change="error = ''" class="w-full border border-slate/30 rounded-lg py-2.5 px-3">
      <option value="">{{ t('moduleQuizView.selectModule') }}</option>
      <option v-for="t2 in topics" :key="t2" :value="t2">{{ t2 }}</option>
    </select>
  </div>

  <p v-if="selectedTopic" class="text-slate text-sm">
    {{ t('moduleQuizView.questionCount', bankCount) }}
  </p>

  <p v-if="error" class="text-coral text-sm">{{ error }}</p>

  <button @click="start" :disabled="!selectedTopic" class="w-full bg-aqua text-white font-medium py-3 rounded-lg disabled:opacity-60">
    {{ t('moduleQuizView.startQuiz') }}
  </button>
</div>
```

Note: the `v-for` loop variable is renamed `t` → `t2` here specifically because `t` is already bound to the i18n translate function in this component's `<script setup>` — using `t` for both would shadow the translator inside that loop. Every other file in this batch keeps `t` as the loop var name where used, since none of the others have this specific collision.

- [ ] **Step 5: Verify**

```bash
npm run build
```
Expected: clean build. Manually load `/module-quiz` as a retail staff account, toggle language, confirm title/labels/placeholder/button switch. Trigger `errorPickModule` (click Start with nothing selected — button is disabled so this one only reachable if you inspect the guard, skip if not easily reachable) and `errorAlreadyAttempted` (pick a topic already attempted today, if a test account has one) to confirm the interpolated `{topic}` renders correctly in both languages.

- [ ] **Step 6: Commit**

```bash
git add lautan-academy-frontend/src/views/ModuleQuizView.vue lautan-academy-frontend/src/i18n/locales/en.json lautan-academy-frontend/src/i18n/locales/ms.json
git commit -m "feat(i18n): migrate ModuleQuizView to vue-i18n"
```

---

### Task 3: Migrate QuizView.vue (and reconcile the local lang toggle)

**Files:**
- Modify: `lautan-academy-frontend/src/views/QuizView.vue`
- Modify: `lautan-academy-frontend/src/i18n/locales/en.json` (add `quizView` key)
- Modify: `lautan-academy-frontend/src/i18n/locales/ms.json` (add `quizView` key)

**Interfaces:**
- Consumes: `useI18n()` (`t`, `locale`), `<LanguageSwitcher />` (Phase 1, `src/components/LanguageSwitcher.vue`).
- Behavior change: the local `lang` ref and its inline toggle button are removed entirely. Question-content language (`_en`/`_ms` field suffix) now follows the shared `locale` from `useI18n()` — same value the rest of the app's UI chrome uses. Switching language via the sidebar (or, mid-quiz, via this page's own `<LanguageSwitcher />`) now changes the quiz question text too, not just UI labels. This is the Phase 1 spec's flagged "open risk" being closed, not a new decision — see `docs/superpowers/specs/2026-08-09-i18n-design.md`.

- [ ] **Step 1: Add `quizView` to en.json**

```json
"quizView": {
  "noActiveQuiz": "No active quiz — join one from the dashboard first.",
  "questionProgress": "Question {current} of {total}",
  "checking": "Checking...",
  "errorCheckFailed": "Could not check that answer — check your connection and try again.",
  "back": "← Back",
  "next": "Next",
  "submitting": "Submitting...",
  "submitQuiz": "Submit quiz",
  "errorSubmitFailed": "Could not submit your answers. Check your connection and try again."
}
```

- [ ] **Step 2: Add matching `quizView` to ms.json**

```json
"quizView": {
  "noActiveQuiz": "Tiada kuiz aktif — sertai satu daripada papan pemuka dahulu.",
  "questionProgress": "Soalan {current} daripada {total}",
  "checking": "Menyemak...",
  "errorCheckFailed": "Tidak dapat menyemak jawapan itu — semak sambungan anda dan cuba lagi.",
  "back": "← Kembali",
  "next": "Seterusnya",
  "submitting": "Menghantar...",
  "submitQuiz": "Hantar kuiz",
  "errorSubmitFailed": "Tidak dapat menghantar jawapan anda. Semak sambungan anda dan cuba lagi."
}
```

- [ ] **Step 3: Rewrite the script block**

Full replacement for the `<script setup>` block in `lautan-academy-frontend/src/views/QuizView.vue`:

```js
<script setup>
// Questions come from sessionStorage (set by DashboardView after a
// successful /quiz/redeem, or ModuleQuizView for Standard Quiz) rather than
// a route param + API fetch.
//
// Server-graded, not client-graded: neither quiz type's question objects
// carry a `correct` field anymore (backend strips it from GET /questions
// and POST /quiz/redeem). Picking an answer calls a live per-question check
// endpoint for the instant reveal — that response is UX only, not
// authoritative. submit() sends the raw {id/index, chosen} answer set and
// the server independently grades the whole attempt from its own stored
// data, so a tampered/faked check response can't change what gets saved.
//
// Question-content language (_en/_ms field suffix) follows the shared
// vue-i18n locale, same as the rest of the app's UI chrome — this used to
// be a separate local `lang` ref with its own toggle button; reconciled
// onto the one shared mechanism (see Phase 1 spec's flagged open risk).
import { ref, computed } from 'vue'
import { useRouter } from 'vue-router'
import { useI18n } from 'vue-i18n'
import { useAuthStore } from '../store/auth'
import { api } from '../api/client'
import LanguageSwitcher from '../components/LanguageSwitcher.vue'

const router = useRouter()
const auth = useAuthStore()
const { t, locale } = useI18n()

const stored = JSON.parse(sessionStorage.getItem('lautan_active_quiz') || 'null')
const topic = stored?.topic || 'Practice'
const passcode = stored?.passcode || ''
const kind = stored?.kind || 'ai' // 'standard' (Module Quiz, ModuleQuizView) | 'ai' (Practice, DashboardView)
const questions = ref(stored?.questions || [])

const currentIndex = ref(0)
const answers = ref({}) // { questionIndex: { chosen, correct, correctIndex } }
const checking = ref(false)
const checkError = ref('')
const submitting = ref(false)
const errorMsg = ref('')

const currentQuestion = computed(() => questions.value[currentIndex.value])
const isLastQuestion = computed(() => currentIndex.value === questions.value.length - 1)
const answeredCount = computed(() => Object.keys(answers.value).length)
const currentAnswer = computed(() => answers.value[currentIndex.value])

function optionsFor(q) {
  const suffix = locale.value === 'en' ? '_en' : '_ms'
  return [q['opt1' + suffix], q['opt2' + suffix], q['opt3' + suffix], q['opt4' + suffix]]
}

// Once an answer is picked for a question it's locked in — matches the
// vanilla app's handleChoice, which disables the option buttons the instant
// one is tapped so the correct/wrong reveal can't be gamed by re-clicking.
const isRevealed = computed(() => currentAnswer.value !== undefined)

async function selectAnswer(optIndex) {
  if (isRevealed.value || checking.value) return
  checkError.value = ''
  checking.value = true
  try {
    const result = kind === 'standard'
      ? await api.checkStandardAnswer(currentQuestion.value.id, optIndex)
      : await api.checkAiAnswer(auth.staff.outlet, passcode, currentIndex.value, optIndex)
    answers.value[currentIndex.value] = { chosen: optIndex, correct: result.correct, correctIndex: result.correctIndex }
  } catch (err) {
    checkError.value = t('quizView.errorCheckFailed')
  } finally {
    checking.value = false
  }
}

function optionClass(i) {
  if (!isRevealed.value) {
    return 'border-slate/20 hover:border-aqua/50'
  }
  if (i === currentAnswer.value.correctIndex) return 'border-aqua bg-aqualight text-deepsea font-medium'
  if (i === currentAnswer.value.chosen) return 'border-coral bg-coral/10 text-coral font-medium'
  return 'border-slate/20 opacity-50'
}

function next() {
  if (!isLastQuestion.value) currentIndex.value++
}
function back() {
  if (currentIndex.value > 0) currentIndex.value--
}

async function submit() {
  submitting.value = true
  errorMsg.value = ''

  const payloadAnswers = questions.value.map((q, i) => {
    const a = answers.value[i]
    return kind === 'standard' ? { id: q.id, chosen: a?.chosen } : { index: i, chosen: a?.chosen }
  })

  try {
    const data = kind === 'standard'
      ? await api.saveResult({ name: auth.staff.name, outlet: auth.staff.outlet, topic, answers: payloadAnswers })
      : await api.saveAiResult({ attemptId: 'AI' + Date.now(), name: auth.staff.name, outlet: auth.staff.outlet, topic, passcode, answers: payloadAnswers })

    // Missed-question summary for ResultView comes from the live per-answer
    // checks already done during the quiz, not recomputed here — the score
    // shown (data.score/data.total/data.percentage) is the server's, though.
    const wrongAnswers = []
    questions.value.forEach((q, i) => {
      const a = answers.value[i]
      if (a && a.chosen !== a.correctIndex) {
        const opts = optionsFor(q)
        wrongAnswers.push({
          qText: locale.value === 'en' ? q.question_en : q.question_ms,
          userChoice: opts[a.chosen] ?? '',
          correctText: opts[a.correctIndex] ?? '',
        })
      }
    })

    sessionStorage.setItem('lautan_last_result', JSON.stringify({ scoreCorrect: data.score, scoreTotal: data.total, percentage: data.percentage, wrongAnswers }))
    sessionStorage.removeItem('lautan_active_quiz')
    router.push('/result')
  } catch (err) {
    errorMsg.value = err.message || t('quizView.errorSubmitFailed')
  } finally {
    submitting.value = false
  }
}
</script>
```

- [ ] **Step 4: Rewrite the template block**

Full replacement for the `<template>` block:

```html
<template>
  <div class="min-h-screen bg-seafoam">
    <div v-if="questions.length === 0" class="p-6 text-coral text-sm">{{ t('quizView.noActiveQuiz') }}</div>

    <div v-else class="max-w-lg mx-auto px-6 py-8">
      <div class="flex items-center justify-between mb-4">
        <span class="text-slate text-xs">{{ t('quizView.questionProgress', { current: currentIndex + 1, total: questions.length }) }}</span>
        <LanguageSwitcher />
      </div>

      <div class="w-full bg-white/60 rounded-full h-1.5 mb-6">
        <div class="bg-aqua h-1.5 rounded-full transition-all duration-300" :style="{ width: ((currentIndex + 1) / questions.length * 100) + '%' }" />
      </div>

      <div class="bg-white rounded-xl2 p-6 shadow-sm">
        <p class="font-display font-semibold text-ink text-lg mb-5">
          {{ locale === 'en' ? currentQuestion.question_en : currentQuestion.question_ms }}
        </p>

        <div class="space-y-3">
          <button
            v-for="(opt, i) in optionsFor(currentQuestion)"
            :key="i"
            @click="selectAnswer(i)"
            :disabled="checking"
            class="w-full text-left px-4 py-3 rounded-lg border transition-colors disabled:opacity-70"
            :class="optionClass(i)"
          >
            {{ opt }}
            <span v-if="isRevealed && i === currentAnswer.correctIndex" class="ml-1">✓</span>
          </button>
        </div>

        <p v-if="checking" class="text-slate text-xs mt-3 text-center">{{ t('quizView.checking') }}</p>
        <p v-if="checkError" class="text-coral text-xs mt-3 text-center">{{ checkError }}</p>
      </div>

      <p v-if="errorMsg" class="text-coral text-sm mt-4 text-center">{{ errorMsg }}</p>

      <div class="flex items-center justify-between mt-6">
        <button
          @click="back"
          :disabled="currentIndex === 0"
          class="text-slate text-sm disabled:opacity-30"
        >
          {{ t('quizView.back') }}
        </button>

        <button
          v-if="!isLastQuestion"
          @click="next"
          class="bg-deepsea text-white text-sm font-medium px-6 py-2.5 rounded-lg"
        >
          {{ t('quizView.next') }}
        </button>
        <button
          v-else
          @click="submit"
          :disabled="submitting || answeredCount < questions.length"
          class="bg-coral text-white text-sm font-medium px-6 py-2.5 rounded-lg disabled:opacity-50"
        >
          {{ submitting ? t('quizView.submitting') : t('quizView.submitQuiz') }}
        </button>
      </div>
    </div>
  </div>
</template>
```

- [ ] **Step 5: Verify**

```bash
npm run build
```
Expected: clean build. Manually start a real quiz (Module Quiz or AI Practice), confirm:
1. The `<LanguageSwitcher />` in the quiz header toggles both the UI labels (Question X of Y, Back/Next/Submit) **and** the question/option text together (this is the behavior change — previously these were independent).
2. Answering, checking (correct/wrong reveal), Back/Next navigation, and final submit → ResultView all still work end-to-end exactly as before.
3. No leftover reference to a `lang` ref anywhere (`grep -n "lang" src/views/QuizView.vue` should show nothing but `locale`/unrelated matches).

- [ ] **Step 6: Commit**

```bash
git add lautan-academy-frontend/src/views/QuizView.vue lautan-academy-frontend/src/i18n/locales/en.json lautan-academy-frontend/src/i18n/locales/ms.json
git commit -m "feat(i18n): migrate QuizView to vue-i18n, reconcile local lang toggle onto shared locale"
```

---

### Task 4: Migrate ResultView.vue

**Files:**
- Modify: `lautan-academy-frontend/src/views/ResultView.vue`
- Modify: `lautan-academy-frontend/src/i18n/locales/en.json` (add `resultView` key)
- Modify: `lautan-academy-frontend/src/i18n/locales/ms.json` (add `resultView` key)

**Interfaces:**
- Consumes: `useI18n()`.

- [ ] **Step 1: Add `resultView` to en.json**

```json
"resultView": {
  "noResult": "No result found.",
  "backToDashboard": "Back to My Learning",
  "passedTitle": "Well done!",
  "practiceTitle": "Keep practicing",
  "scoreLine": "You scored {correct} out of {total}",
  "missedQuestions": "Missed questions",
  "questionPrefix": "Q: {text}",
  "correctLabel": "✓ Correct: {text}"
}
```

- [ ] **Step 2: Add matching `resultView` to ms.json**

```json
"resultView": {
  "noResult": "Tiada keputusan dijumpai.",
  "backToDashboard": "Kembali ke Pembelajaran Saya",
  "passedTitle": "Syabas!",
  "practiceTitle": "Terus berlatih",
  "scoreLine": "Anda mendapat {correct} daripada {total}",
  "missedQuestions": "Soalan tersilap",
  "questionPrefix": "S: {text}",
  "correctLabel": "✓ Betul: {text}"
}
```

- [ ] **Step 3: Rewrite ResultView.vue**

Full replacement:

```vue
<script setup>
import { computed } from 'vue'
import { useRouter } from 'vue-router'
import { useI18n } from 'vue-i18n'
import ProgressRing from '../components/ProgressRing.vue'

const router = useRouter()
const { t } = useI18n()
const result = JSON.parse(sessionStorage.getItem('lautan_last_result') || 'null')

const passed = computed(() => result && Number(result.percentage) >= 70)

function backToDashboard() {
  router.push('/')
}
</script>

<template>
  <div class="min-h-screen bg-deepsea flex items-center justify-center px-6">
    <div v-if="!result" class="text-white text-center">
      <p>{{ t('resultView.noResult') }}</p>
      <button @click="backToDashboard" class="mt-4 text-aqualight underline">{{ t('resultView.backToDashboard') }}</button>
    </div>

    <div v-else class="bg-white rounded-xl2 p-8 max-w-sm w-full shadow-xl">
      <div class="text-center">
        <ProgressRing :percent="Math.round(result.percentage)" :size="120" :accent="passed ? '#1E88C7' : '#E8622C'" />

        <h1 class="font-display text-xl font-semibold text-ink mt-5">
          {{ passed ? t('resultView.passedTitle') : t('resultView.practiceTitle') }}
        </h1>
        <p class="text-slate text-sm mt-1">
          {{ t('resultView.scoreLine', { correct: result.scoreCorrect, total: result.scoreTotal }) }}
        </p>
      </div>

      <div v-if="result.wrongAnswers?.length" class="mt-6 text-left space-y-2 max-h-60 overflow-y-auto">
        <p class="text-xs font-semibold text-slate uppercase tracking-wide">{{ t('resultView.missedQuestions') }}</p>
        <div v-for="(w, i) in result.wrongAnswers" :key="i" class="bg-seafoam rounded-lg p-3">
          <p class="text-xs font-medium text-coral">{{ t('resultView.questionPrefix', { text: w.qText }) }}</p>
          <p class="text-xs text-aqua font-semibold mt-1">{{ t('resultView.correctLabel', { text: w.correctText }) }}</p>
        </div>
      </div>

      <button
        @click="backToDashboard"
        class="mt-6 w-full bg-aqua text-white font-medium py-3 rounded-lg hover:bg-deepsea transition-colors"
      >
        {{ t('resultView.backToDashboard') }}
      </button>
    </div>
  </div>
</template>
```

- [ ] **Step 4: Verify**

```bash
npm run build
```
Expected: clean build. Complete a real quiz to land on `/result`, toggle language, confirm the pass/fail title, score line, "Missed questions" section (if any wrong answers), and both "Back to My Learning" buttons (empty-result and normal state) all switch.

- [ ] **Step 5: Commit**

```bash
git add lautan-academy-frontend/src/views/ResultView.vue lautan-academy-frontend/src/i18n/locales/en.json lautan-academy-frontend/src/i18n/locales/ms.json
git commit -m "feat(i18n): migrate ResultView to vue-i18n"
```

---

### Task 5: Migrate QuizHistoryView.vue

**Files:**
- Modify: `lautan-academy-frontend/src/views/QuizHistoryView.vue`
- Modify: `lautan-academy-frontend/src/i18n/locales/en.json` (add `quizHistoryView` key)
- Modify: `lautan-academy-frontend/src/i18n/locales/ms.json` (add `quizHistoryView` key)

**Interfaces:**
- Consumes: `useI18n()`.

- [ ] **Step 1: Add `quizHistoryView` to en.json**

```json
"quizHistoryView": {
  "title": "Quiz History",
  "loading": "Loading...",
  "moduleQuizHeading": "Module Quiz",
  "noModuleHistory": "Nothing here yet — take a module quiz from the sidebar.",
  "questionPrefix": "Q: {text}",
  "correctLabel": "✓ Correct: {text}",
  "aiPracticeHeading": "AI Practice",
  "noAiHistory": "Nothing here yet — your first practice attempt will show up after you join a code.",
  "assessmentHeading": "Assessment Review",
  "noAssessments": "No assessments filed for you yet.",
  "allYears": "All years",
  "allTopics": "All topics",
  "noAssessmentsFiltered": "No assessments match this filter.",
  "filedBy": "Filed by {manager}",
  "quizScoreLabel": "Quiz score:",
  "competencyLabel": "Competency:",
  "productKnowledge": "Product knowledge",
  "performanceGaps": "Performance gaps",
  "recommendations": "Recommendations",
  "today": "Today",
  "yesterday": "Yesterday",
  "daysAgo": "{days} days ago"
}
```

- [ ] **Step 2: Add matching `quizHistoryView` to ms.json**

```json
"quizHistoryView": {
  "title": "Sejarah Kuiz",
  "loading": "Memuatkan...",
  "moduleQuizHeading": "Kuiz Modul",
  "noModuleHistory": "Belum ada apa-apa di sini — cuba kuiz modul dari bar sisi.",
  "questionPrefix": "S: {text}",
  "correctLabel": "✓ Betul: {text}",
  "aiPracticeHeading": "Latihan AI",
  "noAiHistory": "Belum ada apa-apa di sini — percubaan latihan pertama anda akan dipaparkan selepas anda menyertai kod.",
  "assessmentHeading": "Semakan Penilaian",
  "noAssessments": "Belum ada penilaian difailkan untuk anda.",
  "allYears": "Semua tahun",
  "allTopics": "Semua topik",
  "noAssessmentsFiltered": "Tiada penilaian sepadan dengan penapis ini.",
  "filedBy": "Difailkan oleh {manager}",
  "quizScoreLabel": "Skor kuiz:",
  "competencyLabel": "Kecekapan:",
  "productKnowledge": "Pengetahuan produk",
  "performanceGaps": "Jurang prestasi",
  "recommendations": "Cadangan",
  "today": "Hari ini",
  "yesterday": "Semalam",
  "daysAgo": "{days} hari lalu"
}
```

- [ ] **Step 3: Update the script block**

Add the import, `t`, and use it inside `relativeTime()`:

```js
import { ref, computed, onMounted } from 'vue'
import { useI18n } from 'vue-i18n'
import { api } from '../api/client'
import { useAuthStore } from '../store/auth'
import ProgressRing from '../components/ProgressRing.vue'

const { t } = useI18n()
const standardHistory = ref([])
```

```js
function relativeTime(iso) {
  const days = Math.floor((Date.now() - new Date(iso)) / 86400000)
  if (days <= 0) return t('quizHistoryView.today')
  if (days === 1) return t('quizHistoryView.yesterday')
  if (days < 7) return t('quizHistoryView.daysAgo', { days })
  return new Date(iso).toLocaleDateString()
}
```

(the final fallback — `toLocaleDateString()` for anything 7+ days old — stays as browser-default date formatting, not a translation key; out of scope, matches existing behavior elsewhere in the app.)

- [ ] **Step 4: Update the template**

```html
<h1 class="font-display text-xl font-semibold text-white">{{ t('quizHistoryView.title') }}</h1>
```

```html
<div v-if="loading" class="text-slate text-sm">{{ t('quizHistoryView.loading') }}</div>
```

```html
<h2 class="font-display text-base font-semibold text-ink mb-3">{{ t('quizHistoryView.moduleQuizHeading') }}</h2>
<div v-if="standardHistory.length === 0" class="bg-white rounded-xl2 p-6 text-center">
  <p class="text-slate text-sm">{{ t('quizHistoryView.noModuleHistory') }}</p>
</div>
```

Inside the Module Quiz `<details>` wrong-answer block:
```html
<div v-if="wrongsForStandard(h).length" class="mt-3 space-y-2">
  <div v-for="(w, j) in wrongsForStandard(h)" :key="j" class="bg-seafoam rounded-lg p-3">
    <p class="text-xs font-medium text-coral">{{ t('quizHistoryView.questionPrefix', { text: w['Question Text'] }) }}</p>
    <p class="text-xs text-aqua font-semibold mt-1">{{ t('quizHistoryView.correctLabel', { text: w['Correct Answer'] }) }}</p>
  </div>
</div>
```

```html
<h2 class="font-display text-base font-semibold text-ink mb-3">{{ t('quizHistoryView.aiPracticeHeading') }}</h2>
<div v-if="aiHistory.length === 0" class="bg-white rounded-xl2 p-6 text-center">
  <p class="text-slate text-sm">{{ t('quizHistoryView.noAiHistory') }}</p>
</div>
```

Inside the AI Practice `<details>` wrong-answer block (same shape as Module Quiz's, reuses the same keys):
```html
<div v-if="wrongsForAi(h.AttemptID).length" class="mt-3 space-y-2">
  <div v-for="(w, j) in wrongsForAi(h.AttemptID)" :key="j" class="bg-seafoam rounded-lg p-3">
    <p class="text-xs font-medium text-coral">{{ t('quizHistoryView.questionPrefix', { text: w['Question Text'] }) }}</p>
    <p class="text-xs text-aqua font-semibold mt-1">{{ t('quizHistoryView.correctLabel', { text: w['Correct Answer'] }) }}</p>
  </div>
</div>
```

```html
<h2 class="font-display text-base font-semibold text-ink mb-3">{{ t('quizHistoryView.assessmentHeading') }}</h2>
<div v-if="reports.length === 0" class="bg-white rounded-xl2 p-6 text-center">
  <p class="text-slate text-sm">{{ t('quizHistoryView.noAssessments') }}</p>
</div>
<template v-else>
  <div class="flex flex-wrap gap-2 mb-3">
    <select v-model="reportYear" class="border border-slate/30 rounded-lg py-2 px-3 text-sm bg-white">
      <option value="ALL">{{ t('quizHistoryView.allYears') }}</option>
      <option v-for="y in reportYears" :key="y" :value="y">{{ y }}</option>
    </select>
    <select v-model="reportTopic" class="border border-slate/30 rounded-lg py-2 px-3 text-sm bg-white">
      <option value="ALL">{{ t('quizHistoryView.allTopics') }}</option>
      <option v-for="t3 in reportTopics" :key="t3" :value="t3">{{ t3 }}</option>
    </select>
  </div>
  <div v-if="filteredReports.length === 0" class="bg-white rounded-xl2 p-6 text-center">
    <p class="text-slate text-sm">{{ t('quizHistoryView.noAssessmentsFiltered') }}</p>
  </div>
```

(same `t` → `t2`/`t3` loop-var rename reasoning as Task 2 — this file's `<script setup>` already binds `t` to the translator, so the `reportTopics` `v-for` loop var is renamed to avoid shadowing it.)

Inside the report `<details>`:
```html
<div class="flex-1 min-w-0">
  <p class="text-sm font-medium text-ink truncate">{{ r['Training Title'] }}</p>
  <p class="text-xs text-slate">{{ t('quizHistoryView.filedBy', { manager: r.Manager }) }}</p>
</div>
```

```html
<div class="bg-seafoam rounded-lg p-3">
  <p class="text-xs text-slate">{{ t('quizHistoryView.quizScoreLabel') }} <span class="font-medium text-ink">{{ r['Quiz Score'] }}</span> · {{ t('quizHistoryView.competencyLabel') }} <span class="font-medium text-ink">{{ r.Fluency ?? '—' }}/10</span></p>
</div>
<div v-if="r['Product Knowledge Comments']" class="bg-seafoam rounded-lg p-3">
  <p class="text-xs font-medium text-ink">{{ t('quizHistoryView.productKnowledge') }}</p>
  <p class="text-xs text-slate mt-1 whitespace-pre-wrap">{{ r['Product Knowledge Comments'] }}</p>
</div>
<div v-if="r['Performance Gaps']" class="bg-seafoam rounded-lg p-3">
  <p class="text-xs font-medium text-ink">{{ t('quizHistoryView.performanceGaps') }}</p>
  <p class="text-xs text-slate mt-1 whitespace-pre-wrap">{{ r['Performance Gaps'] }}</p>
</div>
<div v-if="r.Recommendations" class="bg-seafoam rounded-lg p-3">
  <p class="text-xs font-medium text-ink">{{ t('quizHistoryView.recommendations') }}</p>
  <p class="text-xs text-slate mt-1 whitespace-pre-wrap">{{ r.Recommendations }}</p>
</div>
```

All other markup (dynamic bindings, `ProgressRing`, `dateBadge`, `skillLevelColor`, structural `v-if`/`v-for`) stays exactly as-is.

- [ ] **Step 5: Verify**

```bash
npm run build
```
Expected: clean build. Manually load `/quiz-history` as a retail staff account with real history in all three sections (Module Quiz, AI Practice, Assessment Review — the third only shows for `division === 'retail'`), toggle language, confirm every heading/empty-state/filter-option/label switches, including the interpolated "Filed by {manager}" and the wrong-answer Q/Correct pairs in both Module Quiz and AI Practice sections.

- [ ] **Step 6: Commit**

```bash
git add lautan-academy-frontend/src/views/QuizHistoryView.vue lautan-academy-frontend/src/i18n/locales/en.json lautan-academy-frontend/src/i18n/locales/ms.json
git commit -m "feat(i18n): migrate QuizHistoryView to vue-i18n"
```

---

### Task 6: Migrate ResourcesView.vue

**Files:**
- Modify: `lautan-academy-frontend/src/views/ResourcesView.vue`
- Modify: `lautan-academy-frontend/src/i18n/locales/en.json` (add `resourcesView` key)
- Modify: `lautan-academy-frontend/src/i18n/locales/ms.json` (add `resourcesView` key)

**Interfaces:**
- Consumes: `useI18n()`. Reuses Phase 1's `sidebar.roleOutletManager` / `roleWarehouseManager` / `roleAreaManager` / `roleSupervisor` keys for the manager-role header label instead of adding new ones.

- [ ] **Step 1: Add `resourcesView` to en.json**

```json
"resourcesView": {
  "title": "Browse Courses",
  "loading": "Loading...",
  "noMaterial": "No course material added yet.",
  "allCategories": "All categories",
  "allTopics": "All topics",
  "createQuiz": "Create Quiz",
  "createQuizFromThis": "Create Quiz from this",
  "openAttachedLink": "Open attached link"
}
```

- [ ] **Step 2: Add matching `resourcesView` to ms.json**

```json
"resourcesView": {
  "title": "Semak Kursus",
  "loading": "Memuatkan...",
  "noMaterial": "Belum ada bahan kursus ditambah.",
  "allCategories": "Semua kategori",
  "allTopics": "Semua topik",
  "createQuiz": "Cipta Kuiz",
  "createQuizFromThis": "Cipta Kuiz daripada ini",
  "openAttachedLink": "Buka pautan lampiran"
}
```

- [ ] **Step 3: Update the script block**

Add the import/`t`, and replace `ROLE_LABELS` with a lookup into the existing `sidebar.role*` keys (Phase 1) instead of a second copy of the same 4 strings:

```js
import { ref, computed, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { useI18n } from 'vue-i18n'
import { api } from '../api/client'
import { useAuthStore } from '../store/auth'

const driveResources = ref([])
const knowledgeEntries = ref([])
const loading = ref(true)
const route = useRoute()
const auth = useAuthStore()
const { t } = useI18n()
// Dashboard's Browse Courses cards link here with ?category=... so the
// filter is already applied on arrival, not a plain unfiltered landing.
const categoryFilter = ref(route.query.category || 'ALL')
const subcategoryFilter = ref('ALL')
```

```js
const createQuizPath = computed(() => auth.manager?.role === 'warehouse_manager' ? '/warehouse-manager' : '/manager')
const canCreateQuiz = computed(() => ['outlet_manager', 'warehouse_manager'].includes(auth.manager?.role))

// Reuses Phase 1's sidebar role labels (src/i18n/locales/{en,ms}.json ->
// sidebar.roleOutletManager etc.) instead of a second copy of the same 4
// strings under a new resourcesView key.
const ROLE_KEYS = { outlet_manager: 'sidebar.roleOutletManager', warehouse_manager: 'sidebar.roleWarehouseManager', area_manager: 'sidebar.roleAreaManager', supervisor: 'sidebar.roleSupervisor' }
const headerLabel = computed(() => auth.isStaff ? auth.staff?.outlet : (ROLE_KEYS[auth.manager?.role] ? t(ROLE_KEYS[auth.manager?.role]) : ''))
```

- [ ] **Step 4: Update the template**

```html
<p class="text-aqualight text-xs">{{ headerLabel }}</p>
<h1 class="font-display text-xl font-semibold text-white">{{ t('resourcesView.title') }}</h1>
```

```html
<div v-if="loading" class="text-slate text-sm">{{ t('resourcesView.loading') }}</div>
<div v-else-if="allEntries.length === 0" class="bg-white rounded-xl2 p-6 text-center">
  <p class="text-slate text-sm">{{ t('resourcesView.noMaterial') }}</p>
</div>
```

```html
<select v-model="categoryFilter" @change="onCategoryChange" class="border border-slate/30 rounded-lg py-2 px-3 text-sm bg-white">
  <option value="ALL">{{ t('resourcesView.allCategories') }}</option>
  <option v-for="c in categories" :key="c" :value="c">{{ c }}</option>
</select>
<select v-if="subcategories.length" v-model="subcategoryFilter" class="border border-slate/30 rounded-lg py-2 px-3 text-sm bg-white">
  <option value="ALL">{{ t('resourcesView.allTopics') }}</option>
  <option v-for="s in subcategories" :key="s" :value="s">{{ s }}</option>
</select>
```

Drive-backed entry row:
```html
<RouterLink v-if="canCreateQuiz" :to="{ path: createQuizPath, query: { sourceType: 'resource', sourceValue: e.driveId, topicLabel: e.name } }"
  class="text-xs text-white font-medium bg-aqua rounded-full px-3 py-1 shrink-0">
  {{ t('resourcesView.createQuiz') }}
</RouterLink>
```

Knowledge entry row:
```html
<a v-if="e.link" :href="e.link" target="_blank" rel="noopener" class="text-xs text-aqua font-medium underline">{{ t('resourcesView.openAttachedLink') }}</a>
<RouterLink v-if="canCreateQuiz" :to="{ path: createQuizPath, query: { topic: e.subcategory } }" class="text-xs text-white font-medium bg-aqua rounded-full px-3 py-1">
  {{ t('resourcesView.createQuizFromThis') }}
</RouterLink>
```

All other markup (`e.category`/`e.subcategory`/`e.kind`/`e.name`/`e.body` — all real data, not translated) stays as-is.

- [ ] **Step 5: Verify**

```bash
npm run build
```
Expected: clean build. Manually load `/resources` as (a) a staff account — header shows the outlet code, unaffected by the role-label change — and (b) at least one manager role (e.g. Outlet Manager) — header should show the translated role label matching what the sidebar already shows for that role. Toggle language, confirm title/loading/empty-state/filter options/both create-quiz buttons/"Open attached link" all switch, and that Create Quiz links are still only visible for Outlet/Warehouse Manager (not Area Manager/Supervisor/staff).

- [ ] **Step 6: Commit**

```bash
git add lautan-academy-frontend/src/views/ResourcesView.vue lautan-academy-frontend/src/i18n/locales/en.json lautan-academy-frontend/src/i18n/locales/ms.json
git commit -m "feat(i18n): migrate ResourcesView to vue-i18n, reuse sidebar role labels for header"
```

---

## Batch completion check

After Task 6:
- [ ] Run the EN/BM key-parity check used in Phase 1 (every key in `en.json` has a matching key in `ms.json` and vice versa) — confirm clean, same method as Phase 1's verified step.
- [ ] `npm run build` clean end-to-end.
- [ ] Update `MEMORY.md`: mark this batch done, list which of the ~22 Phase 2 views remain (Outlet Manager group, Warehouse+Area Manager group, Supervisor group — see the three batches proposed but not yet planned).
